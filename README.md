# 🍱 Hệ thống Quản lý Bếp ăn Thông minh (Smart Kitchen Management)
### 📍 Dự án Quản lý Bếp ăn KV2 - VT NET

![Django](https://img.shields.io/badge/django-%23092E20.svg?style=for-the-badge&logo=django&logoColor=white)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-316192?style=for-the-badge&logo=postgresql&logoColor=white)
![Bootstrap](https://img.shields.io/badge/bootstrap-%23563D7C.svg?style=for-the-badge&logo=bootstrap&logoColor=white)
![AI Gemini](https://img.shields.io/badge/AI-Gemini-blue?style=for-the-badge&logo=google-gemini&logoColor=white)

---

## 🌟 Giới thiệu
Hệ thống Quản lý Bếp ăn là giải pháp toàn diện giúp tự động hóa quy trình vận hành bếp ăn nội bộ công ty. Dự án tích hợp trí tuệ nhân tạo (AI) để phân tích dinh dưỡng, lên thực đơn tự động và hệ thống thông báo qua Bot (Mattermost/NetChat), giúp tối ưu hóa chi phí và nâng cao trải nghiệm người ăn.

## ✨ Tính năng chính

### 1. Dashboard & AI Analysis
*   **Tổng quan Real-time:** Theo dõi số lượng người đăng ký, chi phí thu-chi và chênh lệch tài chính trong ngày.
*   **AI Dinh dưỡng:** Tự động phân tích hàm lượng Calo và đưa ra lời khuyên sức khỏe dựa trên thực đơn thực tế qua Gemini AI.
*   **Xu hướng tài chính:** Biểu đồ so sánh thu-chi theo tuần/tháng/năm bằng Chart.js.

### 2. Quản lý Đăng ký & Điểm danh
*   **Import Đăng ký:** Hỗ trợ import danh sách đăng ký ăn hàng ngày từ file Excel.
*   **Gửi thông báo Bot:** Tự động gửi tin nhắn nhắc nhở/xác nhận qua NetChat tới từng nhân viên.
*   **Điểm danh thực tế:** Tab "Tham gia" hiển thị danh sách người ăn thực tế được đồng bộ qua API từ hệ thống điểm danh bên ngoài.

### 3. Quản lý Thực đơn & Món ăn
*   **Kho món ăn:** Quản lý chi tiết món ăn (loại món, hình ảnh, mô tả).
*   **Công thức & Nguyên liệu:** Khai báo định mức nguyên liệu chi tiết cho từng món ăn để tự động tính toán khối lượng cần mua dựa trên số người đăng ký.
*   **AI Menu Suggestion:** AI gợi ý thực đơn 5 ngày trong tuần dựa trên các tiêu chí cân bằng dinh dưỡng và sự đa dạng.
*   **Quy trình phê duyệt:** Thực đơn và món ăn mới do bếp trưởng tạo sẽ được Admin phê duyệt trước khi hiển thị.

### 4. Quản lý Kho & Tài chính
*   **Đặt hàng (Purchase):** Tạo đơn hàng nguyên liệu chính và đơn hàng phụ (gia vị, dầu ăn...).
*   **Quản lý Chi phí:** Ghi nhận và phân loại mọi chi phí phát sinh, hỗ trợ quy trình phê duyệt chi phí minh bạch.
*   **Cấu hình giá:** Linh hoạt thiết lập giá suất ăn theo biến động thị trường.

### 5. Hệ thống & Profile
*   **Phân quyền (RBAC):** Phân chia vai trò rõ ràng giữa Admin, Nhân viên bếp và Người ăn.
*   **Đăng nhập OTP:** Người dùng đăng nhập bằng mã OTP nhận qua NetChat (thay cho mật khẩu).
*   **Cấu hình runtime trong Profile Admin:** Bot NetChat (URL/Token), AI Gemini (model + API key), và mẫu tin nhắn OTP/đặt cơm.

## 🛠 Công nghệ sử dụng
*   **Backend:** Python 3.12, Django 5.x
*   **Database:** PostgreSQL
*   **Frontend:** HTML5, CSS3 (Vanilla + Bootstrap 5), JavaScript (ES6+)
*   **AI Integration:** Google Generative AI (Gemini Pro)
*   **Deployment:** Ubuntu Server, Nginx, Gunicorn
*   **Communication:** NetChat API (Mattermost compatible)

## 📂 Cấu trúc thư mục
```text
├── accounts/          # Quản lý người dùng, profile và phân quyền
├── core/              # Các tính năng lõi, Dashboard và AI services
├── meals/             # Quản lý thực đơn, món ăn và định mức
├── finance/           # Quản lý thu chi, hóa đơn và giá suất ăn
├── registrations/     # Quản lý đăng ký và tham gia thực tế
├── reviews/           # Hệ thống đánh giá công khai qua QR Code
├── templates/         # Giao diện người dùng (HTML Templates)
├── static/            # Files tĩnh (CSS, JS, Images)
└── media/             # Lưu trữ hình ảnh món ăn, avatar
```

## 🚀 Cài đặt & Chạy thử

1. **Clone dự án:**
   ```bash
   git clone https://github.com/Thanghv37/bep-an.git
   cd bep-an
   ```

2. **Cài đặt môi trường ảo & Thư viện:**
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows use `venv\Scripts\activate`
   pip install -r requirements.txt
   ```

3. **Cấu hình Database:**
   Chỉnh sửa thông số DATABASE trong `config/settings.py` cho phù hợp với PostgreSQL của bạn.

4. **Migrate & Chạy server:**
   ```bash
   python manage.py migrate
   python manage.py runserver
   ```

## 📝 Nhật ký thay đổi

### 2026-06-08
- **Màn chúc mừng sinh nhật: nâng cấp hoành tráng (TV-safe)** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). Overlay đẹp hơn: nền **gradient party nhiều lớp** (hồng/tím/cam), card bo tròn lớn + bóng đổ + **pop bounce**, thêm **bánh kem 🎂 nhún nhẹ** trên tiêu đề, ảnh đại diện to (220px) có vòng sáng, tên 54px, người hiện **trượt lên lần lượt**. Hiệu ứng: **confetti rơi (24) + bóng bay bay lên (10)** — đều HỮU HẠN (`forwards`) và gỡ DOM sau 25s; chỉ bánh kem nhún infinite nhưng bị giới hạn (overlay `display:none` khi tắt → không vẽ). Chỉ template, không cần migrate. *(Phần sinh nhật thật cần đã `migrate` trên server; xem thử: `/tv/lite/?bday=1`.)*

- **Bỏ nút TV SHOW trên dashboard + thêm nút Thoát trong TV SHOW** ([templates/core/dashboard.html](templates/core/dashboard.html), [templates/core/tv_lite.html](templates/core/tv_lite.html)). Gỡ hẳn block `page_title_action` (nút "TV SHOW" ở header dashboard) vì đã có mục TV SHOW ngoài sidebar. Trang TV SHOW thêm nút **"✕ Thoát"** ở góc phải header (cạnh đồng hồ) + phím **ESC** để quay về dashboard. Chỉ template, không cần migrate.

- **TV SHOW: nền sinh động cho panel Hôm nay + AI** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). Panel "Thực đơn hôm nay" (nền cam→lá) và "AI dinh dưỡng" (nền tím→cyan) thêm **gradient nhiều lớp** (2 radial + 1 linear) cho có chiều sâu; các card (danh sách, stage món, card AI) làm **hơi trong suốt** (`rgba(255,255,255,.66-.7)`) để màu nền ánh qua. Gradient đều **TĨNH** (không animate nền → không tốn GPU/crash). Chỉ template, không cần migrate.

- **TV SHOW: tinh chỉnh panel tuần + hôm nay** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). (1) Panel **Thực đơn tuần** hiện **lần lượt rõ T2→T6** (mỗi card cách nhau 0.25s); ô món **cao hơn, dãn đều lấp đầy** chiều cao ô ngày (`ul` flex + `li` flex:1, padding to hơn). (2) Panel **Thực đơn hôm nay**: danh sách bên trái **hiện dần từng món** đúng nhịp món đang chiếu bên phải (thêm dần thay vì dựng sẵn cả list), món mới nổi bật + tự cuộn theo. Chỉ template, không cần migrate.

- **TV SHOW: thêm màu sắc + điểm nhấn + lời khuyên đổi luân phiên** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). (1) **Màu**: nền gradient tĩnh nhẹ (indigo→cyan), 4 KPI card có tông màu riêng (xanh dương/lá/đỏ/tím), viền tiêu đề + icon mỗi panel theo màu (tuần=indigo, hôm nay=lá, AI=tím-hồng). (2) **Chữ hiện lần lượt**: phân tích AI gõ **typewriter**, mô tả món hiện theo từng chữ (sẵn có). (3) **Điểm nhấn**: gạch chân tên món **chạy ra** mỗi lần đổi món; số điểm danh bump. (4) **Lời khuyên dinh dưỡng tự đổi** 9 câu, luân phiên mỗi 6s với hiệu ứng trượt. Vẫn không animation infinite nặng (chỉ accent nhỏ dot/tag), gradient đều TĨNH. Mốc fallback nếu TV crash: commit `e8b5076`. Chỉ template, không cần migrate.

- **TV SHOW: thêm hiệu ứng dashboard (one-shot + accent nhỏ)** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). KPI card hiện vào lần lượt khi tải; số điểm danh **nảy nhẹ (bump)** mỗi khi đổi giá trị (poll); panel tuần **reveal từng món lần lượt**; mô tả món panel hôm nay **hiện dần theo từng chữ**; ảnh món thêm **vignette** cinematic. Accent chạy liên tục nhưng chỉ trên phần tử bé: **dot active** + **tag HÔM NAY** đập nhẹ (tag chỉ chạy khi panel tuần hiện do panel ẩn dùng display:none). Vẫn không dùng animation infinite nặng/gradient-text/box-shadow lớn. Chỉ template, không cần migrate.

- **TV SHOW: bật lại chúc mừng sinh nhật (bản nhẹ TV-safe)** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). Overlay chúc mừng quay lại trên trang TV mới: poll `/birthday/pending/` mỗi 15s, khi có nhân viên sinh nhật đã điểm danh (đủ 10 phút) → hiện overlay ~25s (ảnh + tên + lời chúc), gộp nhiều người 1 màn. Khác bản cũ (gây crash): confetti **hữu hạn** (`animation: ... forwards`, 18 mảnh) và **gỡ khỏi DOM** sau khi tắt, không dùng animation infinite, nền màu đặc (không blur). Xem thử nhanh: mở `/tv/lite/?bday=1`. **Cần migrate trên server** (cột `date_of_birth` + bảng `BirthdayGreetingLog`) thì endpoint mới chạy; chưa migrate thì overlay không hiện (JS đã bắt lỗi, không crash). Chỉ template.

- **TV SHOW: tăng cỡ chữ ~20% + thêm hiệu ứng sinh động (vẫn 1-lần, TV-safe)** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). Bơm toàn bộ cỡ chữ lên ~20% (KPI 52→62px, kcal 100→120px, tên món 54→64px, đồng hồ 50→60px...). Thêm hiệu ứng **chạy 1 lần** (không infinite): ảnh món **zoom kiểu Ken Burns** mỗi lần đổi món (transform, không filter), tên/badge/mô tả **trượt hiện lần lượt**, **số đếm nhảy lên** (suất đăng ký khi mở + kcal khi vào panel AI), món đang chiếu **nảy nhẹ** + tự cuộn vào tầm nhìn, icon tiêu đề panel pop khi đổi panel. Giữ nguyên tắc TV-safe (không animation infinite/gradient-text/box-shadow lớn). Chỉ template, không cần migrate.

- **TV SHOW nâng cấp: chuyển panel mượt + giới thiệu từng món (ảnh to + mô tả)** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). (1) **Hiệu ứng**: fade khi đổi panel + card xuất hiện lần lượt (chỉ animation **1 lần**, không infinite); thêm vài gradient nhỏ trên logo/icon/dots/progress + dải màu trái mỗi KPI card cho đẹp. (2) **Panel "Hôm nay"** đổi từ lưới sang **giới thiệu từng món**: cột trái là danh sách món (mục đang chiếu nổi bật), cột phải là **ảnh to + tên + mô tả** từng món, tự xoay vòng (mỗi món ~5s) rồi sang panel AI. Cách này còn **nhẹ hơn lưới** vì mỗi lúc chỉ giữ **1 ảnh to** trong DOM. Vẫn giữ TV-safe (panel ẩn `display:none`, không gradient-text, không animation infinite, không box-shadow lớn). Chỉ template, không cần migrate.

- **TV SHOW (`/tv/lite/`) dựng lại bố cục giàu giống trang TV cũ — nhưng TV-safe** ([templates/core/tv_lite.html](templates/core/tv_lite.html)). Sau khi xác nhận bản text-only chạy được trên Samsung TV, dựng lại layout đầy đủ: header + đồng hồ, **4 KPI card** (Đăng ký + thống kê điểm danh, Thu/Chi/Chênh lệch tách thực phẩm/gia vị), **carousel 3 panel** (Thực đơn tuần / Thực đơn hôm nay CÓ ẢNH / AI dinh dưỡng) + dots + progress bar. Giữ kỷ luật chống crash: panel ẩn dùng `display:none` (chỉ 1 panel được paint), cỡ chữ vừa (KPI 52px, kcal 100px thay vì 180px), phẳng — không gradient-text, không animation infinite, không box-shadow lớn/animate, không Bootstrap. Ảnh món hiển thị ở panel hôm nay (dashboard thường vốn có ảnh và chạy tốt trên TV nên an toàn). Auto-reload 8 phút xả bộ nhớ. Chỉ template, không cần migrate.

- **Sidebar "TV SHOW" + đổi nút Chế độ TV sang trang TV nhẹ** ([templates/base.html](templates/base.html), [templates/core/dashboard.html](templates/core/dashboard.html)). Thêm mục **TV SHOW** vào sidebar (cùng cấp Dashboard, ngay dưới Dashboard, chỉ admin+kitchen) trỏ tới trang TV độc lập tối giản `/tv/lite/` (`tv_lite`, đã thêm trước đó). Nút "Chế độ TV" trên dashboard cũng đổi trỏ từ `/tv/` (bản nặng gây crash Samsung TV) sang `/tv/lite/` và đổi nhãn thành "TV SHOW". Lý do: `/tv/` (fullscreen, 3 panel chồng nhau, chữ 180px, nhiều hiệu ứng) crash renderer Tizen trong khi dashboard thường + trang lite nhẹ thì chạy tốt. Chỉ template, không cần migrate.

- **Cảnh báo camera nhận diện TẮT trong giờ trưa qua NetChat** ([core/management/commands/alert_camera_offline.py](core/management/commands/alert_camera_offline.py), [scripts/camera-offline-alert.service](scripts/camera-offline-alert.service), [scripts/camera-offline-alert.timer](scripts/camera-offline-alert.timer), [DEPLOY.md](DEPLOY.md)). Management command mới `alert_camera_offline`: nếu đang trong khung **11:00–12:30 (giờ VN)** mà camera không gửi heartbeat **> 2 phút** (coi như tắt — kiểm theo `RecognitionHeartbeat` mới nhất của mọi camera) thì gửi tin **NetChat DM cảnh báo cho user `thanghv37`**. Người nhận: tra `UserProfile` theo employee_code/username `thanghv37` lấy email → username NetChat (phần trước @), không có thì dùng luôn chuỗi `thanghv37`. Cooldown 30 phút (lưu `SystemConfig['camera_offline_alert_last_at']`) để không spam khi camera tắt lâu. Mọi tham số **fix cứng** trong code. Chạy định kỳ mỗi phút bằng **systemd timer `camera-offline-alert.timer`** (command tự check khung giờ + cooldown nên đa số lần fire no-op). Test: `manage.py alert_camera_offline --test` (gửi tin thử) / `--force` (bỏ qua khung giờ, gửi nếu đang tắt). **Cần làm trên server**: pull → symlink + `systemctl daemon-reload` + `enable --now camera-offline-alert.timer` (xem DEPLOY.md). Không cần migrate.

- **Chống crash renderer trang TV trên Samsung TV — đợt cắt tải nặng #2** ([templates/core/tv_dashboard.html](templates/core/tv_dashboard.html)). Triệu chứng: mở `/tv/` trên TV Samsung vẫn báo "render process has crashed" (cả dashboard). Nguyên nhân chính: nhiều hiệu ứng chạy **liên tục vô hạn** (`animation ... infinite`) bắt GPU vẽ lại mỗi khung hình + bộ nhớ phình dần (cộng với việc bản trước lỡ bỏ auto-reload). Đã xử lý: (1) **bỏ animate `box-shadow`** ô "HÔM NAY" (`tvTodayPulse`) → giữ viền/đổ bóng tĩnh; (2) **bỏ 5 hiệu ứng nền infinite**: 2 blob radial-gradient (`tv-ai-blob` → ẩn), quầng sáng kcal 480×220 (`tvKcalGlow` → ẩn), icon mẹo 120px (`tvTipGlow` → tĩnh), 5 emoji đồ ăn bay (`tvFoodFloat` → gỡ cả markup + CSS); (3) **Ken Burns** bỏ `filter: brightness` (tạo offscreen buffer tốn GPU), chỉ giữ zoom nhẹ bằng `transform`; (4) **bỏ chữ gradient còn sót** (`background-clip:text`) ở số đếm danh sách + counter món → màu đặc; (5) **bỏ Bootstrap CSS + bootstrap-icons** khỏi `<head>` (trang TV không dùng layout Bootstrap, chỉ 1 icon → thay nút Thoát bằng ký tự `✕`) → giảm parse CSS + tải font ngoài; (6) **nền body** từ 3 lớp gradient toàn màn → màu đặc `#f5f7fb`; (7) **khôi phục auto-reload mỗi 5 phút** — reload toàn trang là cách đáng tin để giải phóng bộ nhớ renderer Tizen. Giữ lại các hiệu ứng xuất hiện 1 lần (fade chuyển panel, reveal món). Chỉ template, không cần migrate. *(Phần overlay chúc mừng sinh nhật vẫn còn confetti/animation nặng — sẽ tối ưu riêng sau; tạm chưa nên bấm "Test chúc mừng" trên TV.)*

- **Thực đơn hôm nay: >4 món thì dồn lên 1 hàng ngang** ([templates/core/dashboard.html](templates/core/dashboard.html)). Trước: grid `auto-fit` luôn wrap xuống nhiều hàng (7 món bị xuống 5+2). Giờ: ≤4 món giữ nguyên (card cap 340px, căn giữa); >4 món thêm class `today-dish-grid--row` + set `grid-template-columns: repeat(N, minmax(140px,1fr))` inline (N = số món) để tất cả nằm trên **1 hàng**, card co lại cho vừa; màn hẹp thì cuộn ngang (`overflow-x:auto`) thay vì xuống dòng. Chỉ CSS/template, không cần migrate.

- **Upload nhạc chúc mừng trong trang Profile của admin** ([accounts/views.py](accounts/views.py), [templates/accounts/user_profile.html](templates/accounts/user_profile.html), [core/views.py](core/views.py), [templates/core/tv_dashboard.html](templates/core/tv_dashboard.html)). Thêm card **"🎂 Nhạc chúc mừng sinh nhật"** (chỉ admin) ở trang Profile: upload file `.mp3/.wav/.ogg/.m4a` → lưu vào `media/birthday_audio/`, đường dẫn ghi trong `SystemConfig` (key `birthday_audio`), có nút nghe thử + nút Xóa (quay về file mặc định). Helper `get_birthday_audio_url()` (core) build URL từ media; `_render_dashboard` truyền `birthday_audio_url` ra context. Trang TV ưu tiên dùng nhạc admin upload, **fallback** `static/audio/birthday.mp3` nếu chưa upload. Không cần migrate (dùng `SystemConfig` sẵn có).

- **Màn hình chúc mừng sinh nhật tự động trên TV (`/tv/`)** ([accounts/models.py](accounts/models.py), [accounts/forms.py](accounts/forms.py), [accounts/views.py](accounts/views.py), [templates/accounts/user_list.html](templates/accounts/user_list.html), [core/models.py](core/models.py), [core/views.py](core/views.py), [core/urls.py](core/urls.py), [templates/core/tv_dashboard.html](templates/core/tv_dashboard.html)). (1) **Quản lý người dùng** thêm trường **Ngày sinh** (`UserProfile.date_of_birth`, có cả năm) — thêm vào `UserCreateForm`/`UserUpdateForm` (widget `<input type=date>`), form tự render field mới, thêm cột "Ngày sinh" trong danh sách người dùng. (2) **Logic**: nhân viên có ngày sinh trùng **ngày + tháng** hôm nay, đã **điểm danh** (`AttendanceLog`) hôm nay, và đã qua **10 phút** kể từ lần điểm danh **đầu tiên** → đủ điều kiện chiếu chúc mừng. Endpoint mới `GET /birthday/pending/` (`birthday_pending_api`, guard `can_manage_menu`) tính danh sách "đến hạn & chưa chiếu", trả tên + ảnh đại diện, đồng thời ghi `BirthdayGreetingLog(employee_code, greeting_date)` để **mỗi người chỉ chúc 1 lần/ngày** (chống lặp mỗi nhịp poll). (3) **TV**: poll endpoint cùng nhịp 10s; khi có người → hiện overlay nhẹ (ảnh + tên + lời chúc cố định + bóng bay emoji CSS) trong **~25 giây** rồi tự tắt; nhiều người đến hạn cùng nhịp poll thì **gộp chung 1 màn**. Tuân thủ ràng buộc TV Samsung (không blur/fixed-bg/gradient-text lớn/canvas). (4) **Nhạc**: phát `static/audio/birthday.mp3` khi chiếu. Vì trình duyệt chặn tự phát, thêm nút **"Bật âm thanh chúc mừng"** (người mở TV bấm 1 lần để mở khóa) và **bỏ auto-reload toàn trang mỗi 5 phút** — thay bằng làm mới dữ liệu qua AJAX (swap `.tv-kpi-row`, `#tvWeekGrid`, `#tv-dishes-json`) để trang sống liên tục, nhạc không bị mất quyền phát. **Cần chạy**: `migrate` (2 migration: `accounts/0009`, `core/0009`) + đặt file `static/audio/birthday.mp3` + `collectstatic` + **restart** service Django.

### 2026-06-05
- **Sửa lỗi "Render process has crashed" của trang TV (`/tv/`) trên Samsung Smart TV** ([templates/core/tv_dashboard.html](templates/core/tv_dashboard.html)). Triệu chứng: mở `/tv/` trên TV Samsung đời mới → màn hình trắng + báo "Render process has crashed!". Nguyên nhân: tiến trình render của trình duyệt Tizen hết bộ nhớ/GPU vì trang quá nặng đồ hoạ. Giảm tải bằng cách: (1) bỏ `background-attachment: fixed` ở body; (2) bỏ toàn bộ `filter: blur()` ở các "blob" glow (radial gradient vốn đã mềm sẵn nên nhìn không khác mấy); (3) bỏ `filter: drop-shadow` animate trên icon mẹo 💡; (4) đổi chữ gradient cỡ lớn (`background-clip: text`) sang màu đặc cho đồng hồ, số kcal 180px và tên món 72px — `background-clip:text` ở cỡ chữ lớn tạo texture tốn bộ nhớ. Giao diện trên máy tính gần như không đổi, chỉ nhẹ hơn để TV không crash. Chỉ CSS, không cần migrate.

- **Sửa layout "Thực đơn ngày hôm nay" khi ít món** ([templates/core/dashboard.html](templates/core/dashboard.html)). Trước: grid dùng `repeat(auto-fit, minmax(200px, 1fr))` nên khi chỉ có 1-2 món, mỗi card bị kéo giãn ~50% bề rộng toàn trang → ảnh + card to quá khổ nhìn xấu. Giờ: cap bề rộng mỗi card tối đa `340px` (`minmax(240px, 340px)`) + `justify-content: center` để card giữ kích thước gọn và căn giữa hàng. Nhiều món vẫn wrap bình thường. Chỉ CSS, không cần migrate.

- **Thêm tài liệu hướng dẫn tạo BOT & đăng nhập OTP cho người dùng** ([HUONG_DAN_BOT_OTP.md](HUONG_DAN_BOT_OTP.md)). File hướng dẫn ngắn để gửi cho đồng nghiệp: Phần 1 — Admin tạo BOT trên NetChat (bật Bot Account, lấy Token, dán URL + Token vào Profile → Kiểm tra → Lưu, lưu ý username NetChat = phần trước @ của email và nhân viên phải đã đăng nhập NetChat); Phần 2 — nhân viên đăng nhập bằng OTP (nhập mã NV → nhận mã 6 số qua NetChat → xác nhận), kèm bảng quy định (mã hiệu lực 10 phút, gửi lại sau 60s, khoá 15 phút khi sai >10 lần) và checklist xử lý khi không nhận được mã. Chỉ là tài liệu, không động code, không cần migrate.

### 2026-06-02
- **Chế độ TV mode — trang riêng để chiếu trên màn TV nhà ăn** ([core/views.py](core/views.py), [core/urls.py](core/urls.py), [templates/core/tv_dashboard.html](templates/core/tv_dashboard.html), [templates/base.html](templates/base.html), [templates/core/dashboard.html](templates/core/dashboard.html)). Trước: dashboard kích thước nhỏ, người ở xa trong nhà ăn không nhìn được. Giờ: **trang `/tv/` fullscreen riêng**, dark theme tím-cyan-green, font cỡ TV-friendly (kcal 88px, kpi-value 36px, dish name 17-19px). Permission `can_manage_menu` (admin+kitchen) — diner không vào được. **Nút "Chế độ TV"**: thêm vào header cạnh title "Dashboard chung" (qua block `page_title_action` mới thêm vào [base.html](templates/base.html)), gradient indigo-cyan-green + pulse ring vô tận, hidden ở diner via Django template `{% if %}` check role. **Refactor**: dashboard view body wrap vào `_render_dashboard(request, template_name)` helper → cả `dashboard()` và `tv_dashboard()` đều gọi vào, khác template + decorator. **Cấu trúc TV**: header (logo + brand + clock realtime + nút thoát ESC), 4 KPI cards top (Đăng ký / Thu / Chi / Chênh lệch — giữ cùng data như dashboard), **stage rotation 3 panels**: (1) Thực đơn tuần T2→T6 với cascade entrance + reveal từng món tích lũy + ngày HÔM NAY pulse glow indigo; (2) Thực đơn hôm nay grid `auto-fit minmax(220px)` với cascade + Ken Burns ảnh + float lệch pha + **spotlight cycling 2.4s** (mỗi card được lên đèn lần lượt với shimmer sweep + scale 1.04 + gradient text); (3) AI dinh dưỡng 2 cột — kcal counter đếm từ 0 với glow pulse 88px, typewriter summary, mức đánh giá badge, tip rotation 6s với slide animation, food emoji bay nền (🥗🍎🥕🥦🍅). **Mỗi panel chiếu 18s (panel 3: 16s)**, progress bar phía dưới, dots nhấp tay để jump panel. **Auto-reload mỗi 5 phút** để data tươi. **ESC = thoát về dashboard chính**. Realtime poll số người ăn 10s/lần như dashboard chính. Không cần migrate.

- **Tin xác nhận đặt cơm qua NetChat: thêm thực đơn của ngày + link đánh giá** ([config/settings.py](config/settings.py), [core/message_templates.py](core/message_templates.py), [registrations/views.py](registrations/views.py)). Trước: tin xác nhận chỉ có tên + số suất + ngày + bếp. Giờ thêm 2 mục: (1) **Thực đơn hôm nay** — liệt kê các món của ngày target_date, mỗi món 1 dòng với emoji theo loại (🍚 main, 🥬 side, 🍲 soup, 🍮 dessert), sort theo dish_type + sort_order + tên; (2) **Link đánh giá** — URL public `/reviews/public/` để user click vào đánh giá ngay từ NetChat. **Settings**: thêm `SITE_URL = os.getenv('SITE_URL', 'https://net2kitchen.viettel.pro.vn')` để dựng absolute URL (override qua env nếu cần). **Helpers mới** trong `registrations/views.py`: `_build_menu_summary(target_date)` query DailyMenu mới nhất + format string; trả `_(Chưa có thực đơn cho ngày này)_` (markdown italic) nếu chưa có. `_build_review_link()` ghép `SITE_URL` + `reverse('public_review')`, fallback path `/reviews/public/`. **Performance**: compute 1 lần trong `_send_notifications_bg` rồi truyền xuống `_send_one_notification` qua kwargs — không query lại N lần cho N người. **Template `DEFAULT_MEAL`**: thêm block "🍱 Thực đơn hôm nay:\n{menu_summary}" và "⭐ Đừng quên đánh giá món ăn để bếp cải thiện: {review_link}". **VARS_MEAL** thêm 2 biến mới để admin thấy trong UI Profile cấu hình template. Admin nếu đã save template tùy chỉnh trong SystemConfig (KEY_MEAL) sẽ KHÔNG tự cập nhật — cần vào Profile chỉnh template thủ công để dùng 2 biến mới. Không cần migrate.

### 2026-06-01
- **Caption NetChat + Excel summary: "Khách ngoài: N" cũng đổi sang tổng suất** ([participation_export.py](registrations/participation_export.py)). Trước: caption hiện "Khách ngoài: 2" với 2 dòng "→ Khách A — 2 suất; → Bảo vệ — 1 suất" tổng thực 3 suất → user phản hồi đếm phải bằng tổng suất. Caption đổi `len(guests)` → `sum(g['quantity'] for g in guests)`. Excel summary đổi `counts['guest']` → `counts['guest_qty']`. Giờ đồng nhất 3 nơi (KPI dashboard, caption NetChat, Excel summary) đều dùng TỔNG SUẤT cho dòng "Khách ngoài". Field `guest` (count người) trong `count_statuses` giữ lại phòng khi cần ở chỗ khác.

- **KPI Khách ngoài: tính theo tổng suất, không phải số người** ([registrations/participation_export.py](registrations/participation_export.py), [dashboard.html](templates/core/dashboard.html)). 1 khách có thể đặt nhiều suất (vd đặt 3 suất 1 lần), nên hiển thị "Khách ngoài: 1" khi có 3 suất là sai logic ô KPI. Thêm field `guest_qty` vào `count_statuses` = `sum(r['quantity'] for r if status=='guest')`; KPI JS đổi từ `d.guest` → `d.guest_qty`. Caption NetChat + Excel summary vẫn giữ `guest` (count người) như spec gốc user — vì format "Khách ngoài: N → list từng người" thì N là số người mới đúng ngữ nghĩa.

- **Dashboard: cơm luôn đầu list + KPI "Khách ngoài"** ([core/views.py](core/views.py), [dashboard.html](templates/core/dashboard.html)). (1) **Cơm ưu tiên đầu nhóm main**: trong sort key của `ordered_menu_items` thêm `_rice_priority(item)` ở slot thứ 2 (sau `dish_type_order`, trước `sort_order`) — trả 0 nếu tên dish lowercase startswith 'cơm', else 1. Match đúng "Cơm", "Cơm trắng", "Cơm chiên..." nhưng KHÔNG match "cá cơm". Nhóm main vẫn ở trên cùng, trong đó cơm dẫn đầu, các món main khác giữ thứ tự tương đối. (2) **KPI ĐĂNG KÝ thêm row "Khách ngoài"**: trong card kpi-people thêm `<div class="pstat">` thứ 4 với id `ps-guest` + class màu mới `pnum-blue` (#2563eb). API `participation_counts_api` đã trả field `guest` qua `count_statuses` (đã update từ commit trước), JS poll 10s thêm `if (elGuest) elGuest.textContent = d.guest || 0;`. Không động backend, không cần migrate.

- **Đăng ký khách ngoài (không thuộc TTKTKV2) — tên + suất + ghi chú đơn vị/đối tác** ([registrations/models.py](registrations/models.py), [registrations/views.py](registrations/views.py), [registrations/participation_export.py](registrations/participation_export.py), [registration_form.html](templates/registrations/registration_form.html), [registration_list.html](templates/registrations/registration_list.html), [registration_participation.html](templates/registrations/registration_participation.html), migration `0004_mealregistration_note`). **Model**: thêm `note` CharField max=255 blank vào `MealRegistration` — chủ yếu dùng cho khách ngoài để mô tả đơn vị/đối tác (NV nội bộ thường để trống). **View `registration_create`**: parse thêm 3 arrays POST `guest_name[]`, `guest_qty[]`, `guest_note[]`; validate (tên không rỗng, qty ≥ 1); lưu trong cùng atomic transaction với emp_code synthetic `EXT-<uuid4_hex_12>` (đảm bảo unique cho unique_together date+meal+kitchen+code), `source='guest'`, `full_name=name`, `note=note`. **Template `registration_form.html`**: thêm section "Khách ngoài" ngay dưới bảng nhân viên — table 3 cột (Tên / Số suất / Ghi chú) + nút "Thêm khách ngoài" / "Xóa dòng", section tự ẩn khi không có dòng nào (`#guest-table-wrap` toggle display). Popup xác nhận trước khi lưu cũng liệt kê khách ngoài với badge "Khách ngoài" và highlight nền xanh. **`_build_participation_rows`**: tách `guest_regs` ra khỏi flow chính (không qua scan, không có profile), append cuối với status='guest', type='Khách ngoài', note truyền nguyên. Bổ sung `'note': ''` cho 2 row.append cũ để schema nhất quán. **`_PARTICIPATION_STATUS_LABELS`** thêm key 'guest' → ('Khách ngoài', 'primary'). **`count_statuses`** + **Excel `status_order` / summary / `fills`** thêm guest (fgColor=EFF6FF nhạt xanh). Excel cột "Đơn vị" cho khách ngoài tự đổ note vào (vì profile=None nên cột này trống). **`build_report_caption`** cuối hàm thêm block "Khách ngoài: N" + liệt kê từng người dạng `👉 Nguyễn Văn A (đối tác công ty X) — 2 suất` (note trong ngoặc, nếu rỗng thì bỏ ngoặc). **Trang Tham gia**: dropdown filter status thêm option "Khách ngoài"; cell tên hiển thị note in màu primary thay cho dòng phụ "Lịch sử tham gia ăn" khi status='guest'. **Trang Danh sách đăng ký**: badge "Khách ngoài" màu xanh cạnh tên + dòng phụ thay email bằng note. **Trên server cần `python manage.py migrate registrations` trước khi restart** (migration 0004 thêm field note).

- **Hóa đơn đặt hàng: redesign bảng detail main_order — 2 cột gọn, đẹp như phiếu đặt** ([extra_request_list.html](templates/finance/extra_request_list.html)). Trước: bảng detail 3 cột (Nguyên liệu / Khẩu phần/người / Tổng dự kiến) bám theo header "Nguyên liệu chính cần chuẩn bị cho N người" — rườm rà vì khẩu phần/người ở đây là thông tin tham khảo, không phải nội dung đặt hàng. Giờ: header thành "Phiếu đặt mua nguyên liệu" + subtitle gọn (ngày · N nguyên liệu · cho N người); bảng còn STT + Nguyên liệu + **Số lượng đặt hàng** (đổi từ "Tổng dự kiến"). Style mới: thead nền `#f1f5f9` + border-bottom 2px, body row hover nhẹ xanh, số lượng đặt hàng wrap font-weight 800 màu indigo + unit nhạt bên cạnh, cột STT căn giữa màu xám nhạt 56px width, table có shadow + border-radius 10px nằm trong card gradient trắng→slate. Mobile (≤576px): collapse padding + giảm font. Không động backend, không cần migrate.

- **Phiếu đặt mua nguyên liệu: đơn vị mặc định kg/lít cho đơn số lượng lớn** ([meals/views.py](meals/views.py), [menu_list.html](templates/meals/menu_list.html)). Trước: nguyên liệu khai báo recipe theo g/ml (đơn vị tự nhiên cho khẩu phần 1 người), khi nhân số người ra tổng thì hiện "12150 g" — không hợp lý khi đặt mua số lượng lớn từ nhà cung cấp. Giờ thêm helper `_normalize_to_bulk_unit(items)` quy đổi g/gram → kg, ml/mililit → lít (chia 1000) áp dụng cho cả 2 nhánh: auto-compute từ recipe + prep_order saved (legacy lưu unit='g' vẫn hiện kg, user re-confirm sẽ lưu lại đúng). Áp dụng cho cả khẩu phần/người lẫn tổng đặt hàng (giữ unit nhất quán trong cùng dòng). Đơn vị khác (kg, lít, quả, củ, miếng, gói, bó...) giữ nguyên. **Tránh bug locale vi đổi dấu chấm thành phẩy**: backend format thẳng Decimal sang string sạch (dot separator, strip trailing zeros, no scientific notation) qua helper `_format_decimal_clean`, gán vào dict thay vì để template áp `floatformat` (filter này respect USE_L10N → ra "12,15" làm `<input type="number">` invalid). Template chỉ render string thẳng `{{ item.quantity_per_person }}` / `value="{{ item.required_total_quantity }}"`, không filter. Input `step` từ 0.01 → **0.001** (vì 1g = 0.001 kg, cần precision này khi sửa lại). Test với 81 người: Bột bánh canh 150g/người × 81 = 12150g → "0.15 kg/người · 12.15 kg tổng"; muối 1g/người × 81 = 81g → "0.001 kg/người · 0.081 kg tổng". Không cần migrate.

- **Dashboard: replay tự động entrance animation cho KPI (2 phút) + Thực đơn tuần (1 phút)** ([dashboard.html](templates/core/dashboard.html)). Trước: cascade entrance + counter (KPI) và day cascade + dish reveal (weekly) chỉ chạy 1 lần lúc load → khi treo TV bếp lâu, các hiệu ứng entrance không lặp lại, chỉ còn float/pulse nhẹ. Giờ: (1) **KPI replay mỗi 120s** — function `replayKpi()` đặt `style.animation = 'none'` trên tất cả `.kpi-card`, force reflow qua `void row.offsetWidth`, rồi clear inline animation → stylesheet kick in lại → cascade `kpiSlideUp` chạy fresh từ đầu (stagger qua `--kpi-idx` vẫn hoạt động); sau 700ms gọi `runCounters()` đặt số về 0 và animate lại tới target. **Targets lưu vào `data-counter-target`** ở lần đầu để các lần replay re-dùng (tránh parse lại text sau khi đã thay đổi). (2) **Weekly replay mỗi 60s** — function `replayWeekly()` reset cả `.week-day-card` lẫn `.week-menu-dishes li` cùng lúc, 1 reflow chung trên `#weekly-menu-section`, rồi clear → cả day cascade T2→T6 lẫn từng-món-reveal-tích-lũy chạy lại. Cuối kỳ T2-món1 → ... → T6-mónN xuất hiện lần lượt sau base 900ms + cum * 90ms. **Reduced motion**: cờ `REDUCED_MOTION` module-level, cả 2 interval bị skip; counter vẫn chạy nhưng không stagger. Không động backend, không cần migrate.

### 2026-05-31
- **Dashboard: animation cho KPI cards + reveal lần lượt từng món Thực đơn tuần T2→T6** ([dashboard.html](templates/core/dashboard.html)). Trước: 4 ô KPI (Đăng ký / Thu / Chi / Chênh lệch) tĩnh hoàn toàn sau load; 5 day card thực đơn tuần cũng tĩnh. Giờ: **KPI cards**: (1) cascade entrance L→R, mỗi col gắn `style="--kpi-idx: 0..3"`, `@keyframes kpiSlideUp` (translateY 20 → 0 + scale .96 → 1) stagger 120ms; (2) **counter animate trên số** — JS quét `.kpi-value` + `.kpi-split-val`, parse `firstChild` text-node (tách dấu chấm ngàn / dấu phẩy thập phân, hỗ trợ số âm), đặt về 0 rồi animate `easeOutCubic` về target trong 1.3s qua `requestAnimationFrame`, format lại bằng `toLocaleString('vi-VN')` để giữ định dạng VN. Bỏ qua nếu chứa `.kpi-empty` (Chưa có giá / Chưa có dữ liệu); (3) **float vô tận** sau cascade — `kpiFloat` translateY 0 ↔ -4px chu kỳ 5.5s, stagger 250ms/col → 4 ô "hít thở" lệch pha; (4) **icon bob** — kpiIconBob translateY + rotate ±3deg chu kỳ 3s, hover tăng tốc còn 0.8s; (5) **realtime counter cho ps-valid/ps-notreg/ps-notatt** — override `Object.defineProperty` trên textContent setter của 3 element, mỗi 10s poll trả số mới → animate đếm 600ms từ giá trị hiện tại sang mới thay vì set thẳng. **Weekly menu**: (1) **5 day card cascade T2→T6** — `style="--day-idx: {{ forloop.counter0 }}"`, `weekDayIn` (translateY 18 → 0 + scale .95 → 1) stagger 140ms; (2) **Pulse glow vô tận trên card "Đang xem"** — `selectedPulse` box-shadow nhịp indigo 0.18 ↔ 0.42 + ring 3px chu kỳ 2.6s → mắt tự bị hút đến ngày được chọn; (3) **Từng món hiện lần lượt theo yêu cầu user** — JS tích lũy delay xuyên qua tất cả ngày: T2-món1 → T2-món2 → ... → T2 hết → +3 đơn vị nghỉ → T3-món1 → ..., gán `--reveal-delay` cho từng `<li>`; CSS `weekDishReveal` translateX -12px → 0 + opacity 0 → 1 trong 0.42s; base delay 900ms (đợi day cascade xong), per-dish 90ms, gap 270ms giữa các ngày. Cả 2 phần tôn trọng `prefers-reduced-motion: reduce` (KPI dùng CSS animation tự skip, weekly có check matchMedia trong JS). Không động backend, không cần migrate.
- **Dashboard: AI dinh dưỡng box sinh động — 6 lớp hiệu ứng thu hút** ([dashboard.html](templates/core/dashboard.html)). Trước: AI box gần như tĩnh sau khi load (blob + sparkle có sẵn nhưng yếu). Giờ thêm 6 lớp: (1) **Floating food emoji** — 6 emoji `🥗🍎🥕🥦🍅🥚` bay từ dưới lên trên + rotate 360° + fade in/out (`@keyframes foodFloat` 12-17s/emoji, opacity max 0.22, mỗi cái delay/duration khác nhau, `filter: saturate(.85)` cho dịu mắt); (2) **Counter animate kcal** — số kcal đếm từ 0 → target trong 1.4s với `easeOutCubic`, `requestAnimationFrame`, format VN qua `toLocaleString('vi-VN')`; (3) **Glow pulse số kcal** — `::before` ellipse radial-gradient tím→cyan blur 8px, animation scale .9 ↔ 1.15 + opacity .45 ↔ .9 chu kỳ 2.4s vô tận; (4) **Typewriter summary** — text phân tích gõ ra từng char (18ms/char) sau khi counter chạy 650ms, dùng `insertAdjacentText` (an toàn XSS), `<span class="ai-typing-caret">▍</span>` blink ở cuối — caret biến mất sau khi gõ xong 1.2s; (5) **Speaking wave** — 4 thanh sóng âm linear-gradient indigo→cyan cạnh title, `@keyframes speakBar` (height 4 ↔ 14px chu kỳ 0.95s, stagger 0/0.15/0.30/0.45s) — toggle `.is-active` trong lúc typewriter chạy, đồng nghĩa AI đang "nói"; (6) **Tip rotation animation + tăng tần suất** — đổi từ 60s lên 15s; tip cũ `tip-leave` (translateX -30px + fade) → 450ms sau swap text → `tip-enter` (xuất phát translateX 30px, opacity 0) → reflow → release → slide vào. Bonus: badge icon level (`⚖️/⬆️/⬇️`) bob translateY 0 ↔ -2px + scale 1 ↔ 1.15 chu kỳ 1.8s vô tận. Tất cả hiệu ứng đồng bộ với prefers-reduced-motion thông qua CSS có sẵn. Không động backend, không cần migrate.
- **Dashboard: animation đa lớp "sống" cho card món ăn (chống nhàm trên TV bếp)** ([dashboard.html](templates/core/dashboard.html)). Trước: cascade L→R chỉ chạy 1 lần lúc load → sau đó tĩnh, nhàm khi treo TV. Giờ 4 lớp animation chồng nhau: (1) **Cascade L→R** giữ nguyên (todayDishIn 0.55s, stagger `--idx * 100ms`); (2) **Card float vô tận** — mỗi card nhấp nhô translateY 0 → -6px → 0 chu kỳ 4.5s, stagger `--idx * 220ms` để không đồng bộ → cả grid như "hít thở"; (3) **Ken Burns** trên ảnh — scale 1 → 1.09 + translate -2.5%/-1.5% chu kỳ 14s `alternate`, animation-delay âm theo idx (`-2.3s * idx`) để mỗi ảnh xuất phát ở pha khác nhau, không bao giờ đồng loạt zoom; (4) **Spotlight cycling** — JS `setInterval` mỗi 3.5s "lên đèn" 1 card theo thứ tự L→R, lặp vô tận. Card spotlight: pause float (giữ vị trí ổn định), translateY -10px + scale 1.045, border indigo + box-shadow 22px indigo glow + ring 3px, badge animation `badgePulse` (scale 1 ↔ 1.12), Ken Burns tăng tốc còn 6s, shimmer overlay (`<span class="today-dish-shimmer">` với gradient sweep -100% → 110% trong 1.8s), tên món đổi sang gradient text `#4f46e5 → #06b6d4 → #10b981`. **Accessibility**: media query `prefers-reduced-motion: reduce` ngắt mọi animation; JS check cùng media query để skip spotlight cycle. **Stagger qua CSS custom property**: thay vì inline `animation-delay`, đổi sang `style="--idx: N"` rồi dùng `calc(var(--idx) * 100ms)` trong CSS — cleaner, tái dùng được cho nhiều animation cùng lúc. Không động backend, không cần migrate.
- **Dashboard: tái cấu trúc layout + đổi animation "Thực đơn hôm nay" sang cascade L→R** ([dashboard.html](templates/core/dashboard.html)). Trước: Row 2 weekly(col-8) + today-showcase(col-4, slideshow tự chiếu tổng quan ↔ zoom từng món qua 4.5-5.5s mỗi slide); Row 3 chart(col-8) + AI(col-4). Vấn đề: ô today nhỏ + slideshow tự chuyển → user phải đợi mới xem hết các món. Giờ rearrange thành 3 row: **Row 2** today full-width (col-12) hiển thị TOÀN BỘ món cùng lúc dạng grid `repeat(auto-fit, minmax(200px, 1fr))`; **Row 3** weekly(col-lg-8) + AI(col-lg-4) — AI đứng cạnh weekly thay vì cạnh chart, hợp lý vì AI phân tích menu; **Row 4** chart full-width (col-12) đẩy xuống cuối. Card today mới gồm ảnh 160px height + tên + mô tả (clamp 3 dòng) + badge loại món; animation `todayDishIn` (.55s, translateX -22px → 0 + scale .94 → 1), staggered qua inline `style="animation-delay: {{ forloop.counter0 }}00ms"` — món 1 hiện ngay, món 2 sau 100ms, món 3 sau 200ms... tạo hiệu ứng từng món đi vào lần lượt từ trái sang phải. Xóa toàn bộ CSS `.menu-showcase` + `.showcase-*` (~150 dòng) + JS showcase slideshow (40 dòng) — không còn dùng. Chart height bump 260px → 320px vì col rộng hơn. Mobile (≤768px): today-dish-grid về 2 cột, ảnh 130px. Không động backend, không cần migrate.
- **Phiếu đặt mua nguyên liệu: gate xuất theo trạng thái xác nhận + đổi style nút theo dirty/clean** ([menu_list.html](templates/meals/menu_list.html)). Trước: nút "Xác nhận nguyên liệu" luôn nền xanh đặc, "Xuất ảnh"/"Xuất PDF" luôn enable kể cả khi chưa xác nhận → user xuất phiếu với data chưa khớp DB. Giờ trạng thái UI bám theo "clean" (DB khớp UI) vs "dirty" (chưa xác nhận hoặc đã sửa sau xác nhận): **clean** = nút "Xác nhận" nền xanh đặc (`btn-success`) + nút Xuất ảnh/PDF enable; **dirty** = nút "Xác nhận" viền xanh nền trắng (`btn-outline-success`) + nút Xuất ảnh/PDF disabled + title "Cần xác nhận nguyên liệu trước khi xuất". Template render trạng thái khởi tạo theo `focus_prep_order` (có → clean, không → dirty). JS thêm `markDirty()` / `markClean()` toggle class + disabled state. Trigger dirty: `input` event trên `.prep-qty-input`, click `.prep-del-row`, click "+ Thêm". Trigger clean: POST confirm thành công. Handler "Xuất ảnh" / "Xuất PDF" thêm guard `isDirty()` (check class `btn-outline-success` trên btnConfirm) — kể cả khi devtools bỏ disabled, vẫn alert "Cần xác nhận nguyên liệu trước khi xuất" và return sớm.
- **Phiếu đặt mua nguyên liệu: quy đổi đơn vị khi xuất (g→kg, ml→l ngưỡng 1000)** ([menu_list.html](templates/meals/menu_list.html)). Trước: ảnh xuất hiển thị `12150 g` thô — không pro khi gửi nhà cung cấp. Giờ: thêm `<span class="prep-qty-formatted">` cạnh input, mặc định `display:none`, chỉ hiện khi `.exporting` (CSS); khi capture, JS `formatQuantityForExport(value, unit)` tính lại giá trị quy đổi giống filter `format_weight` ở backend — `g/gram ≥ 1000 → kg`, `ml/mililit ≥ 1000 → l`, đơn vị khác (kg, lít, quả, miếng...) giữ nguyên; số nguyên không kèm `,00`, số lẻ format VN (dấu phẩy thập phân, dấu chấm hàng nghìn). Khi `.exporting` bật, CSS `.exporting .prep-qty-input { display:none }` + `.exporting .prep-unit { display:none }` + `.exporting .prep-qty-formatted { display:inline }` → thay raw input bằng giá trị đẹp. Kết quả: `12150 g` → `12,15 kg`; `4050 g` → `4,05 kg`; `500 g` → `500 g` (giữ nguyên); `2 Lít` → `2 Lít`. Chế độ chỉnh sửa (không xuất): vẫn dùng input thô để bếp nhập số gốc theo đơn vị recipe. Không động backend, không cần migrate.
- **Phiếu đặt mua nguyên liệu: redesign export thành phiếu chuyên nghiệp gửi nhà cung cấp** ([menu_list.html](templates/meals/menu_list.html)). Trước: ảnh xuất từ bảng "Khối lượng cần chuẩn bị" có tiêu đề "BẢNG ĐẶT MUA NGUYÊN LIỆU" + dòng "Cho N người ăn" + cột "Dùng trong món" + cột "Khẩu phần/người" + cột "Tổng cần chuẩn bị" — phù hợp xem nội bộ nhưng không phù hợp gửi nhà cung cấp (lộ số người ăn, format chưa pro). Giờ vùng `#prep-export-region` phân tách edit-view và export-view qua 2 class CSS `.prep-edit-only` (ẩn khi xuất) và `.prep-export-only` (hiện khi xuất, dùng inline `display:none` mặc định). **Khi đang xuất ảnh/PDF** (class `.exporting` bật trong `captureCanvas`): hiển thị header công ty đặt hàng góc trái-trên (Trung tâm Kỹ thuật Khu vực 2 + 27B Nguyễn Thành Hãn, quận Hải Châu, Đà Nẵng) + ngày tháng năm bên phải; tiêu đề chính "PHIẾU ĐẶT MUA NGUYÊN LIỆU"; box nhà cung cấp viền xanh trái (Công ty TNHH Đắc Vinh + 407 Trưng Nữ Vương, quận Hải Châu, Đà Nẵng + dòng "Trung tâm Kỹ thuật Khu vực 2 trân trọng đề nghị..."); bảng chỉ 3 cột STT/Nguyên liệu/Số lượng đặt hàng (cột "Dùng trong món" gắn class `.prep-col-dish` và "Khẩu phần/người" gắn `.prep-col-portion` — CSS `.exporting` ẩn 2 class này); 2 ô chữ ký dưới cùng (Đại diện bên đặt hàng / Đại diện nhà cung cấp). **Khi đang sửa (không xuất)**: vẫn thấy 5 cột đầy đủ + dòng "Cho N người ăn" để bếp tham khảo, không thấy header công ty / chữ ký. **Đổi tên cột**: "Tổng cần chuẩn bị" → "Số lượng đặt hàng" (đổi luôn cả view, vì nội dung là một). Style `.exporting #prep-table` thêm viền 1px rõ ràng + header nền `#f1f5f9` để bảng trông như phiếu in. JS thêm-dòng update để row mới cũng có class `prep-col-dish` / `prep-col-portion` (hidden khi xuất). Tên file vẫn `nguyen_lieu_YYYY-MM-DD.{png,pdf}`. Không động backend, không cần migrate, chỉ restart Django.

### 2026-05-27
- **Xác nhận nguyên liệu cần chuẩn bị: gate hóa đơn theo confirm** ([meals/models.py](meals/models.py), [meals/views.py](meals/views.py), [meals/urls.py](meals/urls.py), [menu_list.html](templates/meals/menu_list.html), [finance/views.py](finance/views.py), migration `0016_menupreporder_menupreporderitem`). Trước: trang "Hóa đơn đặt hàng" tự build main_order từ menu+dishes mỗi lần load → user edit khối lượng trên menu_list không có cách nào lưu lại + không có khái niệm "chốt". Thêm 2 model mới ở meals: `MenuPrepOrder` (OneToOne với DailyMenu, ghi `confirmed_at` + `confirmed_by`) và `MenuPrepOrderItem` (ingredient_name, dish_names CSV, quantity_per_person, quantity, unit, sort_order). View mới `menu_prep_confirm(menu_id)` `@require_POST` nhận JSON `{items: [{name, dish_names, quantity_per_person, quantity, unit}, ...]}`, update_or_create MenuPrepOrder + xóa items cũ + tạo lại (idempotent re-confirm). Endpoint `POST /menus/<menu_id>/prep-confirm/`. **menu_list view**: nếu menu đã có prep_order → render preparation_items từ `prep_order.items` (hiện đúng data đã chốt), ngược lại auto-compute như cũ. Truyền `focus_prep_order` cho template. **menu_list template**: thêm nút xanh "Xác nhận nguyên liệu" cạnh Xuất ảnh/PDF + banner trạng thái (xanh "Đã xác nhận lúc..." / vàng "Chưa xác nhận"). JS `collectItems()` đọc dữ liệu hiện tại trong bảng (kể cả dòng bổ sung) + POST JSON; cập nhật banner ngay không cần reload. **finance/views.py `extra_request_list`**: bỏ logic auto-compute, chỉ build main_order khi `menu.prep_order` tồn tại + lấy items từ prep_order (quantity_per_person + quantity đã có Decimal, dùng `format_main_qty` như cũ). Menu chưa xác nhận → main_order = None → template ẩn dòng (logic `{% if main_order %}` đã có sẵn). **Trên server cần `python manage.py migrate meals` trước khi restart** (migration 0016 tạo 2 bảng mới). Menu cũ đang hiện trên trang Hóa đơn đặt hàng SẼ BỊ ẨN cho đến khi bếp vào lại trang Lên thực đơn và bấm "Xác nhận nguyên liệu" — đây là behavior theo yêu cầu user.
- **Lên thực đơn: bảng nguyên liệu có thể chỉnh sửa + xuất ảnh PNG** ([menu_list.html](templates/meals/menu_list.html)). Trước: bảng "Khối lượng cần chuẩn bị" read-only, chỉ có "Xuất PDF" build từ DB qua backend (`export_ingredients_pdf`), user không can thiệp được. Giờ: (1) cột "Tổng cần chuẩn bị" thành `<input type="number">` viền nét đứt (sửa tại chỗ), (2) mỗi dòng có nút thùng rác để xóa, (3) form ở cuối bảng để **thêm nguyên liệu bổ sung** (tên + khối lượng + đơn vị, hữu ích khi cần thêm gia vị không có trong recipe). STT tự động đánh lại sau mỗi thao tác. **2 nút export client-side**: "Xuất ảnh" → `html2canvas` capture vùng `#prep-export-region` → tải file PNG; "Xuất PDF" → `html2canvas` + `jsPDF` (lazy load qua CDN khi user bấm) → A4 portrait, tự chia trang nếu ảnh dài. Vùng export có header "BẢNG ĐẶT MUA NGUYÊN LIỆU" + ngày + số người ăn. Class `.exporting` (toggle trong lúc capture) ẩn input border + cột thao tác + form thêm để ảnh xuất ra sạch sẽ. File tên `nguyen_lieu_YYYY-MM-DD.{png,pdf}`. Backend view cũ `export_ingredients_pdf` giữ nguyên (không phá link cũ) nhưng UI không còn link tới.
- **Cấu hình gửi báo cáo Tham gia: thêm chọn thứ trong tuần** ([registrations/participation_export.py](registrations/participation_export.py), [registrations/views.py](registrations/views.py), [registrations/management/commands/send_participation_report.py](registrations/management/commands/send_participation_report.py), [registration_participation.html](templates/registrations/registration_participation.html)). Bếp chỉ phục vụ T2-T6 nhưng cron `send_participation_report` đang gửi báo cáo mỗi ngày khi đến giờ. Thêm setting "Ngày trong tuần gửi" để chặn T7/CN. **Backend**: thêm `KEY_SEND_DAYS` + `get_send_days()` / `set_send_days()` trong `participation_export.py`; lưu JSON list weekday `[0..6]` (0=T2 ... 6=CN), mặc định `[0,1,2,3,4]`. View `participation_settings` GET trả `send_days`, POST nhận CSV `send_days=0,1,2,3,4`. Command `send_participation_report` sau khi check khớp giờ → check `now_vn.date().weekday() in get_send_days()`, không khớp thì skip silently. **UI**: modal "Cấu hình gửi báo cáo" có hàng 7 nút toggle T2/T3/T4/T5/T6/T7/CN dùng `btn-check` của Bootstrap; JS load điền checked theo `data.send_days` (fallback [0..4] nếu rỗng), save gửi CSV. Không cần migrate.
- **Báo cáo Tham gia NetChat: liệt kê tên người ở các nhóm cần chú ý** ([registrations/participation_export.py](registrations/participation_export.py)). `build_report_caption` trước chỉ ghi số đếm. Giờ với các nhóm "Đăng ký bổ sung", "Chưa điểm danh", "Chưa đăng ký", "Chưa có hồ sơ (đơn vị khác)" liệt kê thêm danh sách `display_name (employee_code)` dạng `👉 ...` ngay dưới số đếm — người nhận nắm ngay ai vắng/bổ sung không cần mở Excel. Chỉ "Đã điểm danh" giữ nguyên số đếm. Thêm dòng "Tổng suất đăng ký" = tổng `quantity` của tất cả người đăng ký trong ngày (cộng dồn từ rows).
- **Module mới: Quản lý kho (tồn kho tủ lạnh) + lịch sử nhập/xuất** ([finance/models.py](finance/models.py), [finance/views.py](finance/views.py), [finance/urls.py](finance/urls.py), [templates/finance/inventory.html](templates/finance/inventory.html), [base.html](templates/base.html), migrations `0016_inventoryentry` + `0017_inventorylog`). Theo dõi thực phẩm còn lại sau mỗi ngày nấu (mua nhiều nấu không hết → để tủ lạnh). **Models**: `InventoryEntry` (tồn hiện tại, running — ingredient_name, quantity, unit, stored_date="từ ngày" = ngày nhập gần nhất, source, created_by); `InventoryLog` (lịch sử bất biến — action import/export, action_date, ingredient_name, quantity, unit, created_by, created_at). **Mô hình tồn RUNNING gộp theo TÊN + NHÓM ĐƠN VỊ (smart merge)**: helper `_add_inventory` cộng dồn theo `ingredient_name__iexact` VÀ cùng nhóm đơn vị. `_unit_category(unit)` chia: khối lượng (g/kg/kí... base=g), thể tích (ml/l/lít... base=ml) — cùng nhóm thì cộng dồn có quy đổi qua factor (vd 3kg + 500g → 3.5kg; 200ml + 1lít → 1200ml); đơn vị đếm (quả/chai/lon...) mỗi loại là category riêng theo chính tên đơn vị, chỉ gộp khi trùng y hệt (chai + chai gộp; chai + lon hoặc kg + chai = nguyên liệu khác → dòng riêng). Quy đổi về đơn vị của dòng đang có; cập nhật stored_date thành ngày nhập mới; ghi 1 `InventoryLog` import (đơn vị log = đơn vị người nhập gốc). **Nhập**: (1) nút "Thêm" góc phải bảng tồn → modal nhập tay; (2) trích từ hóa đơn — cột phải load `PurchaseExtraItem` ngày được chọn, nút "Lưu tồn" (no-wrap) → modal nhập khối lượng còn lại. **Xuất**: mỗi dòng tồn có 1 nút xuất (icon `box-arrow-right`) → modal nhập khối lượng xuất → `inventory_export` trừ tồn (bọc `transaction.atomic`, chặn xuất quá tồn), ghi `InventoryLog` export; tồn về 0 → xóa dòng. **Bảng tồn**: cột Nguyên liệu / Khối lượng tồn (read-only) / Từ ngày / nút Xuất (đã bỏ sửa-khối-lượng-inline và cột Nguồn theo yêu cầu). **Lịch sử nhập/xuất** hiển thị dưới (50 dòng gần nhất): thời gian, loại (Nhập xanh / Xuất cam), nguyên liệu, khối lượng, ngày, người thực hiện. **Endpoint**: `GET /inventory/`, `POST /inventory/add-manual/`, `POST /inventory/save-from-invoice/`, `POST /inventory/<pk>/export/`. **Quyền**: `can_manage_purchase` (admin + kitchen). **Sidebar**: link "Quản lý kho" (icon box2-heart) ở block admin + kitchen. JS bọc trong `DOMContentLoaded` vì Bootstrap JS load cuối base.html (sau block content) — nếu chạy IIFE ngay sẽ thiếu `bootstrap.Modal`. **Trên server cần `python manage.py migrate finance` trước khi restart** (migration 0016 + 0017).
- **Trang Tham gia: nút Xóa lượt quét (chỉ admin)** ([registrations/views.py](registrations/views.py), [registrations/urls.py](registrations/urls.py), [registration_participation.html](templates/registrations/registration_participation.html)). Thêm view `participation_delete_scan` (`@user_passes_test(is_admin)` + `@require_POST`): xóa `AttendanceLog` + `AttendanceCapture` (ảnh) của 1 mã NV trong 1 ngày. **Giữ nguyên `MealRegistration`** → sau khi xóa, người đó chuyển thành "chưa điểm danh" (nếu có đăng ký) hoặc biến mất khỏi list (nếu chỉ có lượt quét). Dùng khi camera nhận nhầm / quét trùng. Endpoint `POST /registrations/participation/delete-scan/`. UI: cột "Thao tác" mới CHỈ hiện khi `can_delete_scan = is_admin(user)` (kitchen vào trang Tham gia được nhưng không thấy nút), nút trash chỉ render ở dòng có `scan_time` (dòng chưa điểm danh không có gì để xóa → hiện "—"). JS confirm trước khi xóa, reload sau khi xong. Empty-state colspan 6/7 tùy có cột thao tác hay không.
- **Meal Transfer: đổi nội dung NetChat + chặn merge khi B đã đăng ký** ([registrations/meal_transfer.py](registrations/meal_transfer.py), [registrations/meal_transfer_notify.py](registrations/meal_transfer_notify.py), [registrations/views.py](registrations/views.py)). Trước: nếu B đã có MealRegistration cùng (date, meal, kitchen) thì `apply_meal_transfer` **cộng dồn quantity** vào B + xóa của A. Đổi sang **all-or-nothing**: nếu có bất kỳ overlap (date, meal, kitchen) giữa A và B → cancel transfer, không động data, báo cả 2 bên qua NetChat. **Signature mới**: `apply_meal_transfer(transfer)` trả `(status, keys)` với status ∈ `{'applied', 'a_not_registered', 'b_already_registered'}` và keys là list `(meal_name, kitchen_name)`. **Nội dung NetChat mới**: format chuẩn theo yêu cầu user — applied: `Đồng chí {A} đã chuyển suất ăn "{meal}" ngày {date} cho đồng chí {B} tại "{kitchen}"`; failed_b_conflict: thêm hậu tố `không thành công. Lý do: đồng chí {B} đã đăng ký suất ăn "{meal}" ngày {date} tại "{kitchen}"`; failed_a_not_registered (không có meal/kitchen vì A trống): `... không thành công. Lý do: đồng chí {A} chưa đăng ký suất ăn ngày {date}`. **Multi-meal**: nếu A đăng ký nhiều bữa (Bữa trưa + Bữa tối) → mỗi (meal, kitchen) 1 tin riêng cho mỗi bên (4 tin tổng cho 2 bữa). `_build_messages` đổi return type từ dict → list `[(emp_code, msg), ...]` để 1 user nhận được nhiều tin. `send_transfer_netchat` gom theo emp_code → 1 lookup NetChat / user, gửi nhiều tin sequential. **Trigger fail**: tại `meal_transfer_create` view (B conflict khi tạo) và `_apply_queryset` (B conflict khi import Excel hook chạy). `cancel_expired_meal_transfers` dùng event `failed_a_not_registered` thay vì `cancelled` chung chung. Event `cancelled` giữ lại cho luồng user/admin tự bấm hủy.
- **Trang Thêm người đăng ký: hỗ trợ nhập username thay vì mã NV** ([registrations/views.py](registrations/views.py), [registration_form.html](templates/registrations/registration_form.html)). Trước đây ô "Mã nhân viên" chỉ chấp nhận emp_code thuần (vd `483088`). Giờ chấp nhận cả "user" (= email local part, vd `annt830` → tự tra ra `483088`). View build thêm `user_alias_map = {email_local_lower: emp_code}` cùng `user_map` đã có, **chỉ thêm những local part xuất hiện DUY NHẤT** — nếu 2 user khác đơn vị cùng local part (vd `thanghv37@viettel.com.vn` và `thanghv37@viettel.vn`) thì cả 2 KHÔNG vào alias_map, user buộc nhập mã NV chính xác. Server validation: resolve raw input → canonical emp_code (`raw in user_map` → giữ; ngược lại `local = raw.split('@')[0].lower()` → `alias_map[local]`). Lưu canonical vào DB. Check trùng (dedup trong batch + check trùng với DB) dùng canonical. JS: `resolveCode()` cùng logic; khi user nhập username, hiện badge xanh `→ Mã NV: 483088` dưới ô; popup xác nhận hiển thị `483088 (annt830) — Tên` cho dòng nhập username. Header table + placeholder đổi "Mã nhân viên" → "Mã NV hoặc Username".
- **Meal Transfer lookup: thêm match-by-email-prefix + chống nhập nhằng** ([registrations/views.py](registrations/views.py)). Trước: lookup chỉ theo `employee_code` hoặc `User.username` → user gõ `annt830` không ra ai (vì username = employee_code = `483088`, không phải `annt830`). Sau: thêm `Q(email__istartswith=f'{local_part}@')` — tách local part trước `@` rồi match prefix. `annt830` → khớp `annt830@viettel.com.vn`; `annt8300` KHÔNG khớp `annt830@...` (vì sau prefix phải là `@`). Nếu user paste full email → tự cắt local part nên vẫn work. Nếu nhiều profile cùng local part (vd 2 người cùng tên đăng nhập khác domain) → trả lỗi liệt kê mã NV, yêu cầu dùng mã NV chính xác — đúng yêu cầu "chỉ ra 1 người duy nhất". Case-insensitive ở cả 3 nhánh.
- **Module mới: Chuyển suất ăn (Meal Transfer) trong trang Profile** ([registrations/models.py](registrations/models.py), [registrations/meal_transfer.py](registrations/meal_transfer.py), [registrations/meal_transfer_notify.py](registrations/meal_transfer_notify.py), [registrations/views.py](registrations/views.py), [registrations/urls.py](registrations/urls.py), [accounts/views.py](accounts/views.py), [user_profile.html](templates/accounts/user_profile.html), migration `0003_mealtransfer`, management command [cancel_expired_meal_transfers](registrations/management/commands/cancel_expired_meal_transfers.py)). Context: NV A đăng ký ăn nhưng bận, cho NV B ăn suất của mình → hệ thống cũ A bị "chưa điểm danh", B "chưa đăng ký". Feature mới: trước 11h ngày X, A vào trang Profile → section "🍱 Chuyển suất ăn" → chọn ngày + tra cứu B (mã NV / username) → AJAX hiển thị info B → bấm xác nhận. **Model** `MealTransfer`: from_user/code/name, to_user/code/name, meal_date, status (pending/applied/cancelled), cancel_reason, created_at, applied_at. **Apply logic** `apply_meal_transfer()`: lấy mọi MealRegistration của A ngày X → đổi `employee_code` + `full_name` sang B + `source='transfer'`. Nếu B đã có MealRegistration cùng (date, meal_name, kitchen_name) → cộng quantity vào B, xóa của A (tránh vi phạm `unique_together`). Bọc `transaction.atomic()`. **Luồng**: khi A bấm chuyển → check cutoff 11h (`is_within_cutoff` dùng timezone VN) → check trùng (đã có pending/applied cùng ngày) → tạo MealTransfer → thử apply ngay; nếu A chưa có data → giữ pending. **Trigger apply pending** hook trong `registration_import` (sau `import_registrations_from_excel`) gọi `apply_all_pending_transfers()`. **Expiry** management command `cancel_expired_meal_transfers` hủy pending còn lại sau 11h ngày X — chạy thủ công hoặc setup cron sau. **NetChat noti** `send_transfer_netchat(transfer, event)` (event='pending'/'applied'/'cancelled') dùng lookup-by-email (đã test ở session trước work tốt), gửi DM riêng cho A và B với nội dung phù hợp event. Tất cả lỗi mạng / config thiếu được catch + log warning, không làm fail luồng chính. **Endpoint mới**: `GET /registrations/meal-transfer/lookup/?q=` (AJAX info người nhận), `POST /registrations/meal-transfer/create/`, `POST /registrations/meal-transfer/<pk>/cancel/`. Permission: chỉ `@login_required` (mọi role dùng được), chỉ chính người tạo hoặc admin mới hủy được. **UI Profile**: section mới sau profile-info-grid, hiển thị cho mọi role; form bên trái + table "Yêu cầu của tôi" (10 gần nhất, có nút Hủy cho pending) + danh sách "Suất nhận được" bên phải. **Trên server cần `python manage.py migrate registrations` trước khi restart** (migration 0003 thêm bảng MealTransfer). Nếu muốn auto-hủy pending hết hạn: setup cron/timer gọi `python manage.py cancel_expired_meal_transfers` mỗi giờ.
- **Excel Tham gia: summary lên đầu + cột Số suất ĐK + ô Tổng suất ĐK** ([registrations/views.py](registrations/views.py), [registrations/participation_export.py](registrations/participation_export.py)). Context: 1 người có thể đăng ký nhiều suất (đặt giúp người khác), Excel cũ chỉ thấy được tổng số người không thấy tổng suất. (1) Trong `_build_participation_rows` thêm `registered_quantity_map` aggregate `quantity` theo emp_code (cộng dồn nếu cùng MNV có nhiều bản ghi), gắn `quantity` vào mỗi row. (2) `build_excel_bytes` refactor layout: hàng 1 title (merge A1:I1, mở rộng vì có thêm cột), hàng 2 summary inline với 7 ô — Tổng / Đã điểm danh / Đăng ký bổ sung / Chưa điểm danh / Chưa đăng ký / Chưa có hồ sơ / **Tổng suất ĐK** (ô cuối bold), hàng 3 spacer, hàng 4 headers, hàng 5+ data. Header trước có 8 cột → giờ 9 cột: chèn "Số suất ĐK" sau "Mã NV". `total_quantity = sum(r['quantity'])` qua tất cả rows. Widths array cập nhật theo 9 cột.
- **Danh sách đăng ký + Tham gia: xử lý người chưa có hồ sơ (đơn vị khác)** ([registrations/views.py](registrations/views.py), [registration_list.html](templates/registrations/registration_list.html), [registration_participation.html](templates/registrations/registration_participation.html), [participation_export.py](registrations/participation_export.py)). Context: có nhân viên thuộc đơn vị khác trong tổng công ty được bypass vào ăn không cần nhận diện khuôn mặt — họ đăng ký nhưng UserProfile không có, dẫn đến UX nhập nhằng. (1) Trang Danh mục đăng ký: bỏ dòng phụ "Chưa có trong danh sách nhân viên → không gửi được tin" và title hover trùng nội dung — chỉ giữ badge cam "Chưa có hồ sơ NV" cạnh tên. (2) Sort lại: người chưa có hồ sơ lên đầu, sau đó MNV tăng dần trong mỗi nhóm — dùng `rows.sort(key=lambda r: (r['profile'] is not None, employee_code))`. (3) Trang Tham gia: thêm status mới `no_profile` ("Chưa có hồ sơ", class warning màu cam) vào `_PARTICIPATION_STATUS_LABELS`. Trong `_build_participation_rows`, override status thành `no_profile` khi `profile is None` ở cả 2 nhánh (quét thẻ và registered-but-not-scanned) — vì người không có dữ liệu khuôn mặt không thể "chưa điểm danh" theo nghĩa thông thường. (4) `count_statuses` + `build_report_caption` (NetChat report) + Excel export (`status_order`, `fills`) đều bổ sung `no_profile`. Excel fill màu vàng nhạt FEF3C7 khớp UI. Caption NetChat chỉ kèm dòng "Chưa có hồ sơ (đơn vị khác): N" khi có ≥ 1 case (giữ caption gọn cho trường hợp bình thường). (5) Thêm option "Chưa có hồ sơ" vào dropdown filter trạng thái ở trang Tham gia. Không động API `participation_counts_api` / KPI dashboard (3 trường valid/not_registered/not_attended giữ nguyên — `no_profile` được thêm vào JSON nhưng JS không đọc nên không vỡ).
- **Fix phân quyền (phụ): `delete_all_registrations` + `export_ingredients_pdf`** ([registrations/views.py:396](registrations/views.py#L396), [meals/views.py:1163](meals/views.py#L1163)). (1) `delete_all_registrations` đang dùng inline check `if not request.user.is_superuser` → admin role thường (không phải superuser Django) không xoá được toàn bộ đăng ký, gây bất tiện. Đổi sang `if not is_admin(request.user)` dùng helper từ `accounts.permissions` (check role=='admin' OR is_superuser) — nhất quán với phần còn lại của app. Thêm import `is_admin` ở [registrations/views.py:10](registrations/views.py#L10). (2) `export_ingredients_pdf` (xuất PDF danh sách nguyên liệu cần mua theo thực đơn tuần) chỉ `@login_required` → diner cũng xuất được. Thêm `@user_passes_test(can_manage_menu)` để chỉ admin+kitchen xuất được — đồng bộ với các view khác trong app `meals`.
- **Fix phân quyền: thống nhất check role cho danh sách đăng ký + trang Tham gia + yêu cầu mua bổ sung** ([registrations/views.py](registrations/views.py), [finance/views.py](finance/views.py)). Phát hiện qua audit: (1) `registration_list` đang chỉ `@login_required` (decorator phân quyền bị comment lại) → diner cũng xem được toàn bộ danh sách đăng ký công ty; (2) hàm `is_admin` LOCAL trong `registrations/views.py` định nghĩa `return user.is_staff` — trong khi user kitchen được tạo với `is_staff=True` ([accounts/views.py:287](accounts/views.py#L287)) → kitchen "lọt" qua check `is_admin` local này và vào được trang Tham gia, nhưng tên hàm gây hiểu nhầm là "chỉ admin". User xác nhận kitchen ĐƯỢC vào trang Tham gia là đúng ý đồ, nên xoá hàm `is_admin` local nhập nhằng và đổi 7 chỗ `@user_passes_test(is_admin)` → `@user_passes_test(can_manage_menu)` (= admin+kitchen, nhất quán với phần còn lại của app `registrations`). Bật lại `@user_passes_test(can_manage_menu)` cho `registration_list`. Thêm `@user_passes_test(can_manage_purchase)` cho `extra_request_create` và `extra_request_list` ở `finance/views.py` — trước đây chỉ `@login_required` → diner cũng tạo/xem được yêu cầu mua bổ sung. Hành vi thay đổi: diner KHÔNG còn vào được `/registrations/` và `/extra-requests/`; admin+kitchen không đổi (vẫn vào được tất cả). API `/registrations/api/by-date/` và `/participation/counts/` được giữ public theo ý user (camera/bot ngoài gọi vào, KPI dashboard cho cả 3 role). Không cần migrate, chỉ restart Django.
- **Fix: Tạo/sửa món ăn không có nguyên liệu vẫn lưu thành PENDING** ([meals/views.py](meals/views.py)). Trước đây `dish_create` / `dish_update` gọi `dish.save()` trước, sau đó mới `save_dish_ingredients_from_post()` — hàm này raise `ValueError` khi nguyên liệu trống/thiếu trường. `except` chỉ thêm error vào form nhưng dish đã ghi vào DB rồi → món rác status PENDING xuất hiện ở trang Phê duyệt. Bọc cả hai view bằng `with transaction.atomic():` (thêm `from django.db import transaction`) — khi `save_dish_ingredients_from_post` raise ValueError, dish.save() bị rollback, form quay về trang nhập với error "Bạn phải nhập đầy đủ tên nguyên liệu, khẩu phần và đơn vị." hoặc "Bạn phải nhập ít nhất 1 nguyên liệu cho món ăn." Không cần migrate, không cần restart riêng — chỉ cần restart Django sau khi pull code (`sudo systemctl restart <service>` trên server, hoặc Ctrl+C + chạy lại `python manage.py runserver` ở local).

### 2026-05-25
- **Đánh giá món ăn: chuyển từ like/dislike sang số sao 1-5** ([reviews/models.py](reviews/models.py), [migration 0008](reviews/migrations/0008_dishreview_star_rating.py), [reviews/views.py](reviews/views.py), [meals/views.py](meals/views.py), [reports/views.py](reports/views.py), [public_review.html](templates/reviews/public_review.html), [review_dashboard.html](templates/reviews/review_dashboard.html), [dish_list.html](templates/meals/dish_list.html)). `DishReview.evaluation` (CharField like/dislike) → `rating` PositiveSmallIntegerField + validators 1-5. Migration 0008 xóa sạch dữ liệu cũ (user xác nhận: like/dislike không map thẳng sang sao). Hai endpoint AJAX `ajax_review_dish` và `ajax_public_review_dish` đổi payload `evaluation` → `rating`, validate 1-5 qua helper `_parse_rating`. Trang Đánh giá bữa ăn (`review_dashboard.html`): mỗi món có hàng 5 sao bấm + dòng "⭐ avg / 5 · N lượt" cho ngày được chọn (view aggregate `Avg('rating')` + `Count('id')` group by dish_id). Trang Public (`public_review.html`): card dish chuyển layout flex-column, hàng sao 5 nút lớn, click tự lưu — không cho clear, chỉ cho đổi mức. Trang Danh mục món ăn (`dish_list.html`): 2 cột 👍/👎 → 1 cột "Đánh giá" hiển thị `4.3 ⭐ (12)` với sort theo `avg_rating`. Báo cáo Excel (`export_review_report`): sheet "Thống kê đánh giá" đổi columns sang Điểm TB / Tổng lượt / Số 5⭐ / Số 1⭐, sort theo `-avg_rating`. **Trên dev/server cần `python manage.py migrate reviews` trước khi restart — migration xóa toàn bộ DishReview cũ.**
- **Dashboard: đổi nhãn thẻ KPI "Người ăn" → "Người đăng ký"** ([dashboard.html](templates/core/dashboard.html)). Số liệu bên dưới (Đã điểm danh / Chưa đăng ký / Chưa điểm danh) là thống kê quanh việc đăng ký, nên nhãn "Người đăng ký" phản ánh đúng nội dung hơn. Chỉ đổi chữ, không động logic.
- **Form "Thêm món ăn": đơn vị nguyên liệu cho tự nhập + bổ sung loại miếng/cái/con/...** ([dish_form.html](templates/meals/dish_form.html), [meals/views.py](meals/views.py), [unit_filters.py](meals/templatetags/unit_filters.py)). Trước đây ô "Đơn vị" của từng nguyên liệu là `<select>` cứng 6 lựa chọn (g/kg/ml/l/phần/quả) — không hỗ trợ các loại đếm cái như miếng, cái, con và không thể tự nhập đơn vị mới. Đổi sang `<input type="text" list="unitSuggestions">` với 1 `<datalist>` dùng chung — gợi ý khi gõ và vẫn cho phép tự nhập đơn vị bất kỳ. Áp dụng cho cả 3 chỗ: row server-rendered, row mặc định, và template JS `addIngredientRow()`. Datalist hiển thị 1 dòng duy nhất với dạng viết hoa đầu (`Gram`, `Kg`, `Ml`, `Lít`, `Phần`, `Quả`, `Miếng`, `Cái`, `Con`, `Lát`, `Củ`, `Gói`, `Bó`) — không còn 2 dòng `value` / label như datalist tự xếp khi có inner text. `save_dish_ingredients_from_post` viết hoa chữ cái đầu của unit khi lưu nên dù user gõ chữ thường (`miếng`) vẫn ra `Miếng`. `format_weight` filter thêm `_UNIT_ALIASES` (`gram→g`, `lít→l`, ...) để giữ nguyên auto-convert g→kg và ml→l ở ngưỡng 1000, đồng thời preserve casing người dùng nhập khi hiển thị các unit khác (`5 Miếng`, `2 Lít`). `DishIngredient.unit` là CharField max 20 nên không cần migration; dữ liệu cũ ('g', 'kg', 'ml', 'l') vẫn render đúng qua aliases.
- **Trang Phê duyệt: hiện badge "Tạo trễ" / "Đã sửa đổi" + lý do trên thực đơn chờ duyệt** ([meals/views.py](meals/views.py), [approval_dashboard.html](templates/meals/approval_dashboard.html)). Người duyệt cần nắm được vì sao bếp lại nộp một thực đơn trễ hạn hay chỉnh sửa lại. View `approval_dashboard` loop qua `pending_menus` và set `menu.edit_kind`: nếu có `edit_reason` thì so sánh `last_edited_at - created_at` < 300s → `'late_create'`, ngược lại → `'edit'`. Template hiển thị badge tương ứng (đỏ "Tạo trễ" / cam "Đã sửa đổi") cạnh dòng người tạo, và một khối lý do (gradient cam nhạt) với label "Lý do:", timestamp `last_edited_at`, và nội dung `edit_reason` (white-space: pre-wrap để giữ xuống dòng).

### 2026-05-22
- **Sidebar: nhân viên bếp thấy link "Quản lý người dùng"** ([base.html](templates/base.html)). Link đang nằm trong block `{% if admin %}` chung với "Danh sách đăng kí" / "Danh mục món ăn" — kitchen không thấy link nên dù view đã mở quyền vẫn không vào được. Tách link "Quản lý người dùng" thành block riêng với điều kiện `superuser OR admin OR kitchen`, đặt ngay vị trí cũ (đầu khối link admin) để thứ tự hiển thị không đổi với admin.
- **Trang Quản lý người dùng: nhân viên bếp xem được danh sách (chỉ đọc)** ([accounts/permissions.py](accounts/permissions.py), [accounts/views.py](accounts/views.py), [user_list.html](templates/accounts/user_list.html)). Mục đích: nhân viên bếp đối chiếu mã NV ↔ tên khi tra cứu. Thêm `can_view_user_list = admin OR kitchen`; `user_list` view đổi decorator sang permission này, truyền thêm `can_manage = can_manage_user(request.user)` xuống template. Template wrap 2 nút "📄 Import Excel" + "+ Thêm người dùng" và toàn bộ `<tr.user-action-row>` (chứa nút Sửa/Xóa) bằng `{% if can_manage %}` — kitchen không thấy. JS click row thêm null-check `if (actionRow)` để không crash khi không có action row. Các view tạo/sửa/xóa/import vẫn `can_manage_user` (chỉ admin) — kitchen bypass URL sẽ bị chặn.
- **Lịch thực đơn tháng: bỏ cột Thứ 7 và Chủ nhật** ([menu_list.html](templates/meals/menu_list.html)). Bếp ăn chỉ phục vụ T2-T6, không cần hiển thị cuối tuần. Xóa 2 `<th>` cuối + đổi vòng lặp ô ngày sang `{% for day in week|slice:":5" %}` (view dùng `calendar.Calendar(firstweekday=0)` → Mon đầu tuần, slice `:5` lấy đúng T2-T6). Không động view.
- **Chart "Xu hướng trong tuần" chỉ vẽ tới hôm nay** ([core/views.py](core/views.py)). Trước đây vẽ đủ T2-T6 (giá trị 0 cho ngày tương lai → cột trống / điểm 0 gây nhiễu thị giác). Loop tạo data chart kiểm tra `d > date.today()` → append `None` cho cả 6 dataset (income/expense/balance × food/spice) → Chart.js bỏ qua điểm null nên cột bar / line không vẽ. Khung "Thực đơn trong tuần" bên dưới giữ nguyên render đủ 5 ngày.
- **Sửa hóa đơn chi phí: cho tick TP/GV từng món + tự tách 2 phiếu** ([purchase_form.html](templates/finance/purchase_form.html), [finance/views.py](finance/views.py)). Trước đây 2 cột tick TP/GV chỉ có ở màn tạo mới (`{% if is_create %}`) — nếu lúc tạo tick nhầm loại và phiếu đã duyệt thì không sửa được phân loại từng món. Giờ cột TP/GV hiện ở cả màn sửa: rows item hiện có render sẵn checkbox tick theo `purchase_type` của phiếu; JS `aiTypeCellsHtml` bỏ guard `IS_CREATE` nên dòng thêm động / AI-scan đều có cột này; breakdown tổng TP/GV cũng hiện ở màn sửa. Backend `purchase_update` parse `ai_item_type[]`, gom `tp_items`/`gv_items`: nếu phiếu sau sửa có **cả 2 loại** → phiếu hiện tại giữ nhóm trùng `purchase_type` cũ, nhóm còn lại tách ra **phiếu mới** (copy ảnh hóa đơn, status PENDING); cả 2 phiếu về chờ duyệt lại. Nếu chỉ 1 loại → cập nhật phiếu như cũ, cho phép đổi `purchase_type` theo loại đã tick.
- **Dashboard: khóa chiều cao 2 khung thực đơn để không đẩy nội dung dưới ra ngoài màn hình** ([dashboard.html](templates/core/dashboard.html)). `.menu-showcase` có `min-height: 440px` cứng — lớn hơn chiều cao tự nhiên của khung "Thực đơn trong tuần" bên cạnh, nên khi ngày đó có menu (ảnh món) khung "Thực đơn ngày hôm nay" phình to, kéo cả row cao lên, đẩy chart + AI box xuống ngoài tầm nhìn màn hình TV bếp. Đổi `min-height: 0` → showcase (slides đã `position: absolute`) co theo chiều cao khung tuần, ảnh `object-fit: contain` tự scale vừa khung. Thêm media query `max-width: 991.98px` đặt lại `min-height: 380px` cho mobile (2 khung xếp dọc, không còn khung tuần định chiều cao).
- **Dashboard: thẻ "Người ăn" thêm 3 số liệu tham gia realtime** ([dashboard.html](templates/core/dashboard.html), [registrations/views.py](registrations/views.py), [registrations/urls.py](registrations/urls.py)). Endpoint mới `GET /registrations/participation/counts/?date=` trả `count_statuses` của `_build_participation_rows`. Thẻ "Người ăn" bỏ dòng meta "Số suất đăng ký trong ngày", thay bằng 3 dòng compact: Đã điểm danh (xanh) / Chưa đăng ký (cam) / Chưa điểm danh (đỏ); JS poll endpoint mỗi 10s cập nhật. Giữ nguyên kích thước thẻ — 3 dòng dùng layout `space-between` (nhãn trái, số phải), font 11.5px, không có chấm màu để gọn trong cột hẹp `col-lg-2`.
- **Lưu ảnh chụp lúc quét mặt + hiển thị ở trang Tham gia** ([core/models.py](core/models.py), [core/views.py](core/views.py), [registrations/views.py](registrations/views.py), [registration_participation.html](templates/registrations/registration_participation.html)). Client nhận diện gửi kèm ảnh khung hình mỗi khi nhận ra 1 nhân viên. (1) **Model `AttendanceCapture`** (`employee_code`, `camera_id`, `scan_time`, `status`, `score`, `image` ImageField `upload_to=attendance_captures/%Y/%m/%d/`). Migration `0008`. (2) **Endpoint `POST /api/recognition/capture/`** — multipart/form-data, `csrf_exempt`, auth Bearer chung; field `image` + `employee_code` bắt buộc. Tách riêng khỏi `attendance-log` (không gộp — giữ JSON schema điểm danh nguyên vẹn). (3) **Tự xóa ảnh sau 30 ngày**: `_purge_old_captures_if_due()` chạy lazy tối đa 1 lần/ngày (kích bởi request capture đầu tiên trong ngày, dùng `SystemConfig['capture_purge_date']` để idempotent — không cần systemd timer). (4) **Trang Tham gia**: `_build_participation_rows` thêm `capture_image_url` (ảnh mới nhất/người/ngày); template có cột "Ảnh" riêng (giữa "Thời gian quét" và "Trạng thái") hiện thumbnail 64×48, click mở ảnh full ở tab mới. (5) Đăng ký `AttendanceCapture` vào Django admin. Chỉ lưu ảnh người nhận diện ra mã NV (client không gửi "Unknown").
- **Báo cáo Tham gia: sửa giờ cột "Thời gian quét" + thêm số liệu vào tin nhắn** ([participation_export.py](registrations/participation_export.py)). (1) Cột "Thời gian quét" trong Excel bị trễ 7 tiếng: `USE_TZ=True` nên `AttendanceLog.scan_time` lưu UTC, code cũ `.strftime()` thẳng trên datetime aware UTC → ra giờ GMT+0. Fix: `timezone.localtime()` convert sang `Asia/Ho_Chi_Minh` trước khi format. (2) Caption tin nhắn NetChat trước chỉ có "📊 Báo cáo Tham gia ngày DD-MM-YYYY", giờ kèm 4 dòng số liệu gạch đầu dòng (đã điểm danh / đăng ký bổ sung / chưa điểm danh / chưa đăng ký) để người nhận nắm nhanh không cần mở file. Thêm helper `count_statuses()` + `build_report_caption()`; `send_participation_excel()` nhận thêm `rows` để dựng caption; `send_excel_to_recipients/channel` nhận thêm param `message`. 2 caller (view gửi tay + command auto-send) truyền `rows` xuống.
- **Fix lỗi 500 khi tạo món ăn trùng tên** ([meals/forms.py](meals/forms.py)). `Dish.name` là `unique` và `Dish.save()` chuẩn hóa tên qua `normalize_vietnamese_name` (viết hoa chữ đầu, còn lại thường). `DishForm` là ModelForm — `validate_unique` chạy trên giá trị **chưa chuẩn hóa**, nên tên gõ khác kiểu hoa/thường hoặc dư khoảng trắng (vd "CÁ KHO" khi DB có "Cá kho") lọt qua check trùng của form → `dish.save()` chuẩn hóa lại → trùng thật → `IntegrityError`; view `dish_create`/`dish_update` chỉ bắt `ValueError` nên không bắt được → HTTP 500. Fix: thêm `DishForm.clean_name()` chuẩn hóa tên bằng chính `normalize_vietnamese_name` trước khi `validate_unique` chạy → form bắt được trùng và báo lỗi đẹp tại ô "Tên món", không còn 500. Fix 1 chỗ áp dụng cho cả tạo mới lẫn cập nhật.
- **Thêm cấu hình `LOGGING` route lỗi ra stderr** ([config/settings.py](config/settings.py)). Trước đây project không có `LOGGING` config → Django mặc định (DEBUG=False) chỉ gửi traceback 500 qua `mail_admins`, mà không có `ADMINS` nên traceback bị nuốt hoàn toàn — `journalctl -u bep-an` không thấy gì khi có lỗi 500. Thêm `LOGGING` với handler `console` (StreamHandler → stderr), `root` level INFO, logger `django.request` level ERROR → từ giờ mọi traceback 500 sẽ vào journald, debug production được.
- **Popup lịch sử bật/tắt camera ở topbar** ([core/views.py](core/views.py), [core/urls.py](core/urls.py), [base.html](templates/base.html)). Hover hoặc click vào pill "Camera" → hiện popup nhỏ liệt kê lịch sử online/offline (chấm xanh = Bật, đỏ = Tắt, kèm giờ + ngày). Endpoint mới `GET /api/recognition/logs/` (chỉ admin) trả `CameraStatusLog` trong 5 ngày gần nhất, tối đa 10 dòng mới nhất. JS nạp log khi mouseenter; click để ghim popup (cho thiết bị cảm ứng), click ra ngoài để đóng. `#camera-status-widget` tách thành `#camera-pills` (đèn) + `.camera-log-popup` (popup) để render độc lập.
- **Tích hợp theo dõi trạng thái hệ thống nhận diện (camera)** — web bếp ăn hiển thị đèn 🟢/🔴 ở topbar cho admin biết client camera nhận diện còn sống hay không. (1) **Model mới** ([core/models.py](core/models.py)): `RecognitionHeartbeat` (1 row/camera, `camera_id` PK, `last_heartbeat_at`, `last_info` JSON) + `CameraStatusLog` (lịch sử chuyển online↔offline, `changed_at` là thời điểm thực tế của sự kiện). Migration `0007_camerastatuslog_recognitionheartbeat`. (2) **2 endpoint mới** ([core/views.py](core/views.py), [core/urls.py](core/urls.py)): `POST /api/recognition/heartbeat/` (client gửi định kỳ, `csrf_exempt`, xác thực qua header `Authorization: Bearer`) và `GET /api/recognition/status/` (chỉ admin — trả danh sách camera online/offline, đồng thời lazy-detect camera vừa rớt offline rồi ghi log với timestamp = heartbeat cuối + 30s). Ngưỡng offline 30s. (3) **Vá bảo mật `attendance_log_api`**: thêm token check chung với heartbeat. Cơ chế an toàn: khi `recognition_token` chưa cấu hình (rỗng) → server KHÔNG enforce, request cũ vẫn qua → deploy không gây cửa sổ chết điểm danh; chỉ khi admin set token mới bắt buộc. (4) **Trang Hồ sơ cá nhân** ([accounts/views.py](accounts/views.py), [user_profile.html](templates/accounts/user_profile.html)): thêm card "📷 Token hệ thống nhận diện" — admin nhập/sửa `recognition_token` lưu vào `SystemConfig` (giống pattern netchat_token), nút Sửa/Hủy/Lưu. (5) **Topbar widget** ([base.html](templates/base.html)): pill "Camera" + chấm đèn ở góc phải, chỉ render cho superuser hoặc role admin; JS poll `/api/recognition/status/` mỗi 15s, mỗi camera 1 pill (1 camera → nhãn "Camera", nhiều → nhãn theo `camera_id`). (6) **Django admin** ([core/admin.py](core/admin.py)): đăng ký 2 model mới để admin xem lịch sử online/offline qua `/admin/`.
- **Client nhận diện** (folder `Client/` — gitignored, chạy local trên máy client, không thuộc repo web): thêm heartbeat + xác thực. Sửa `config.py` (load `.env` theo đường dẫn cạnh exe — fix bug `os.chdir(_MEIPASS)` khi frozen khiến `load_dotenv()` không thấy `.env`; thêm `RECOGNITION_TOKEN`, `CAMERA_ID`, `HEARTBEAT_*`), `app/log_sender.py` (gắn header `Authorization: Bearer` vào POST điểm danh, log rõ khi HTTP 401), file mới `app/heartbeat.py` (thread daemon gửi heartbeat 10s/lần), `app/__init__.py` (start thread). KHÔNG đụng vào cấu trúc/thuật toán nhận diện theo yêu cầu — chỉ lớp truyền thông.

### 2026-05-21
- **Dashboard: đổi layout 3 thẻ Thu/Chi/Chênh lệch sang dọc để số tiền lớn không bị cắt** ([dashboard.html](templates/core/dashboard.html)). Trước layout ngang: `.kpi-icon-col` (label+icon dọc) bên trái + grid 2 cột (TP|GV) bên phải → khi tổng đăng kí lớn, số như 2.070.000đ vượt chiều rộng cột và bị `text-overflow: ellipsis` cắt thành "2.070.0...". Đổi `.kpi-card-split` sang `flex-direction: column`: header chỉ còn text "THU / CHI / CHÊNH LỆCH" căn giữa sát viền trên card (padding-top 6px), `.kpi-split` giữ grid 2 cột nhưng giờ chiếm full chiều rộng card → mỗi ô TP/GV gấp đôi chiều rộng cũ, số tiền fit thoải mái. Ẩn `.kpi-icon` trong header (CSS `display: none`) để compact chiều cao — HTML icon vẫn còn để dễ revert. Giữ nguyên chiều dài/rộng card (vẫn `col-12 col-md-6 col-lg`, `height: 100%`). Không đụng vào card "Số suất đăng kí" (không có class `kpi-card-split`).
- **Quản lý người dùng: đồng bộ ngoại hình nút "Import Excel" với trang Danh sách đăng kí** ([user_list.html](templates/accounts/user_list.html)) — chỉ cosmetic, giữ nguyên hành vi (vẫn `<a>` dẫn sang trang trung gian `import_users.html`). Đổi `btn-success` → `btn-outline-success` + prefix icon 📄 cho khớp nút bên đăng kí; thêm class `action-btn-custom` cho cả 2 nút Import / + Thêm người dùng để align cùng chiều cao 38px; container đổi sang `action-btn-group` cho nhất quán selector. Copy CSS rule `.action-btn-custom` (height/padding/radius/font-weight) vào `<style>` block của user_list.html (rule này hiện chỉ tồn tại local trong registration_list.html).
- **Gửi tin nhắn đăng ký ăn: thêm retry tối đa 3 lượt cho người gặp lỗi tạm thời** ([registrations/views.py](registrations/views.py)). Trước đây mỗi MNV chỉ gửi đúng 1 lượt, lỗi gì cũng ghi `NotificationLog` failed rồi bỏ; lỗi tạm thời (timeout/5xx/429/mạng) không có cơ hội thử lại. Fix: tách hàm `_send_one_notification` trả về `('success' | 'permanent' | 'retryable', err)` — phân loại 404 / 401 / 403 / "thiếu user id" là **permanent** (bỏ luôn, retry vô ích), còn timeout / 5xx khác / 429 / `RequestException` là **retryable**. Hàm `_send_notifications_bg` rewrite theo mô hình pass-based: vòng ngoài lặp tối đa `MAX_NOTIFICATION_PASSES = 3` lượt, mỗi lượt duyệt list `pending`, ai retryable thì đẩy sang `next_pending` cho lượt sau, giữa các lượt nghỉ 30s. Throttle 60s/15 tin giờ đếm theo `attempt_counter` xuyên suốt các lượt thay vì index của loop. `NotificationLog` chỉ tạo 1 record duy nhất cho mỗi MNV ở cuối job (sau khi đã định đoạt) — tránh tạo nhiều record fail cho cùng 1 người khi retry; record retry-exhausted có prefix `[Đã thử 3 lượt]` trong `error_message`.

### 2026-05-20
- **Fix "Lỗi kết nối" khi bấm Đăng ký bổ sung ở trang Tham gia** ([registrations/views.py](registrations/views.py)). Root cause: `MealRegistration.unique_together = (employee_code, date, meal_name, kitchen_name)` không kèm `source`. Người đã có record từ Excel (source='excel') với `meal='Bữa trưa', kitchen='Bếp khu vực 2'`, khi bấm `+` → `get_or_create(source='supplementary', defaults=...)` lookup không match (khác source) → CREATE → DB constraint vi phạm vì record excel chiếm chỗ → `IntegrityError` → Django HTML 500 → JS `r.json()` fail → alert "Lỗi kết nối". Fix: (1) `participation_add_supplementary` check `filter(emp, date).exists()` trước, nếu đã có record bất kể source thì no-op success (tránh đếm trùng suất ăn). (2) `_build_participation_rows` đổi điều kiện supplementary từ `emp_code in supplementary_codes` (chỉ source='supplementary') sang `emp_code in registered_name_map` (bất kể source) — đồng thời accept cả AttendanceLog status English `'not_registered'` lẫn tiếng Việt `'Chưa đăng ký'` (data thật trong DB là VN, condition cũ chưa từng match). Dọn `supplementary_codes` set không còn dùng.
- **Danh sách chi phí: bảng mặt hàng tách cột SL/Đơn vị + thêm Thành tiền** ([purchase_list.html](templates/finance/purchase_list.html), [finance/models.py](finance/models.py)). Trước bảng có 3 cột: Tên hàng / SL (qty+unit gộp như `0,2 kg`) / Giá. Đổi thành 5 cột: Tên hàng / SL / Đơn vị / Đơn giá / Thành tiền. Thêm property `PurchaseExtraItem.line_total = quantity × unit_price` (làm tròn) để cột Thành tiền render thẳng từ model. SL dùng `floatformat:"-2"` bỏ trailing zeros (`3,00` → `3`, `0,20` → `0,2`).
- **Báo cáo: biểu đồ Chênh lệch thêm mode "Theo ngày trong tuần" + đổi sang cột (bar) cho daily** ([reports/views.py](reports/views.py), [report_dashboard.html](templates/reports/report_dashboard.html)). (1) Thêm option `daily_in_week` ở dropdown loại biểu đồ — X-axis là **5 ngày làm việc T2→T6** của tuần chứa `selected_date` (bỏ T7 + CN). Logic mask future tương tự daily_in_month: chỉ vẽ tới hôm nay, ngày tương lai trong tuần → null. (2) Form filter chart 2 bỏ hidden input, **hiện 3 ô** Ngày / Tháng / Năm, JS `toggleBalanceFilters()` ẩn/hiện theo mode (week: chỉ ngày; month: tháng+năm; year: chỉ năm). User pick độc lập với form chart 1. (3) Chart 2 đổi từ 5 line sang **mixed bar + line**: 2 cột Chênh lệch TP (indigo) / GV (teal) đứng song song mỗi ngày, 3 đường lũy kế giữ nguyên (TP indigo nét đứt, GV teal nét đứt, Tổng đỏ đậm có fill). Màu bar và line cùng loại match nhau (TP indigo cả 2, GV teal cả 2) → dễ liên kết "bar này đóng góp vào line kia". `order` quy định lớp vẽ: bar dưới cùng → line lũy kế con → line Tổng trên cùng. Mode `monthly_in_year` giữ 1 line Tổng như cũ. ([finance/views.py](finance/views.py), [purchase_form.html](templates/finance/purchase_form.html)). Hóa đơn mua hàng giờ có thể gộp cả 2 loại trong 1 lần nhập. (1) Bảng "Các mặt hàng trong hóa đơn" thêm 2 cột **TP** + **GV** (chỉ ở mode CREATE) với checkbox mutex (default TP, theo dropdown "Phân loại"); helper JS `aiTypeCellsHtml()` chèn cell, `toggleItemType()` enforce 1-trong-2. AI scan và `addAiItemRow` đều bơm 2 ô này. `recalcAiTotals` tính thêm subtotal 🍖 TP / 🧂 GV hiển thị dưới bảng. (2) Backend `purchase_create` parse `ai_item_type[]` → group thành `tp_items` / `gv_items`. Trong `transaction.atomic`: cả 2 loại → tạo phiếu TP trước (giữ ảnh upload), rồi tạo phiếu GV với items GV + **copy file ảnh ra path mới** (`upload_to` tự gen tên theo `purchase_type='extra'`), note giống nhau. Chỉ 1 loại → 1 phiếu như cũ, `purchase_type` + `actual_cost` set theo items (override form input). (3) Mode UPDATE không động vào — bill cũ giữ purchase_type cũ. (4) Đổi nút native "Choose File / No file chosen" thành "📁 Chọn ảnh" + text "Chưa chọn ảnh / [tên file]" tiếng Việt: wrap `<input type=file>` trong `<div d-none>` giữ input để form vẫn submit, dùng `<label for=id_bill_image>` btn-outline-primary làm trigger, JS handler update tên file khi user chọn.
- **Phê duyệt: bỏ ghi chú "(tự nhập)"** ở label "Tổng chi phí" trong bảng hóa đơn ([approval_dashboard.html](templates/meals/approval_dashboard.html)) — gọn hơn vì giờ chi phí auto-tính từ items.
- **Danh sách đăng ký: highlight người chưa có hồ sơ trong danh sách nhân viên** [registration_list.html](templates/registrations/registration_list.html). Tiếp nối thay đổi "Chưa gửi" — nguyên nhân thường là người đăng ký chưa được tạo `UserProfile` (chỉ có tên free-text, không có mã NV/email → không gửi NetChat được). View đã có sẵn `profile_map` cho từng row; chỉ cần check `{% if not profile %}` ở template để mark dòng đó: nền vàng `#fffbeb` (hover `#fef3c7`), viền cam 3px ở mép trái, avatar đổi từ 👤 sang ⚠️ trong khung vàng, badge "Chưa có hồ sơ NV" cạnh tên, subtext email thay bằng text cam "Chưa có trong danh sách nhân viên → không gửi được tin", thêm `title` tooltip cho cả dòng. Không đụng backend.
- **Log gửi tin nhắn: thêm danh sách "Chưa gửi"** ([registrations/views.py](registrations/views.py), [registration_list.html](templates/registrations/registration_list.html)). Trước đây box log chỉ có Thành công / Thất bại — nếu tổng đăng ký ≠ (success + failed) thì user không biết ai bị bỏ sót (vd 88 đăng ký, success=87, failed=0 → 1 người mất tích). (1) API `get_notification_logs_api` query thêm `MealRegistration` của ngày đó để dựng `registered_names` (dedup theo `employee_code`), trừ đi các emp_code đã có log → ra `pending` list; trả thêm `pending_count` + mảng `pending: [{employee_code, full_name}]`. (2) Box log thêm dòng "Chưa gửi" badge vàng dưới dòng Thất bại. (3) Modal chi tiết thêm tab thứ 3 "Chưa gửi (N)" với bảng `Mã NV / Họ tên` + alert hướng dẫn bấm "Gửi tin nhắn" lại để gửi nhóm này. JS render đồng bộ count cho cả box và modal.
- **Cấu hình giá: cho phép sửa giá ngày trong quá khứ + tô vàng các ngày bị sửa hậu kỳ** ([core/models.py](core/models.py), [core/views.py](core/views.py), [meal_price_list.html](templates/core/meal_price_list.html)). (1) `MealPriceSetting.clean()` bỏ luôn rule "không được set start_date < today" — workflow thực tế: bếp nhập lại giá đã thực thi nhưng quên log, hoặc bổ sung dữ liệu cũ. Chỉ giữ rule `end_date >= start_date`. (2) Form input bỏ `min` attr → datepicker trên browser cho chọn mọi ngày. (3) Lịch tổng quan năm ở [meal_price_list](templates/core/meal_price_list.html): mỗi ô ngày, nếu xuất hiện trong **bất kỳ** `MealPriceChangeLog` nào (cả `create` lẫn `update`) tại thời điểm `changed_at` mà ngày đó đã thuộc về quá khứ → tô vàng `#fef3c7` viền cam, có tooltip. Logic loại bỏ trường hợp "tạo cấu hình cho khoảng tương lai rồi thời gian trôi qua" (vì `d >= changed_date` thì không mark). Subtitle phía trên lịch năm thêm chú thích ô vàng để user biết ý nghĩa.
- **Trang Báo cáo: tách Thu/Chi/Chênh lệch theo Thực phẩm & Gia vị** ([reports/views.py](reports/views.py), [report_dashboard.html](templates/reports/report_dashboard.html)). (1) Helper `build_purchase_split_map` tách chi phí theo phân loại hóa đơn; `report_dashboard` rewrite loop dùng `get_price_breakdown_for_date` → mỗi `daily_rows` row thêm `food_price/spice_price/income_food/income_spice/expense_food/expense_spice/balance_food/balance_spice`; context có thêm các series tương ứng và total subtotals. (2) **3 stat card** trên cùng đổi sang layout chia đôi (icon-col bên trái có nhãn + icon + tổng, bên phải 2 ô 🍖 Thực phẩm / 🧂 Gia vị), tổng vẫn hiển thị bên cạnh icon. (3) **Chart 1 (Thu-Chi)** đổi từ 3 cột (Thu/Chi/Chênh) sang **cột chồng** — cột Thu (TP+GV) + cột Chi (TP+GV) cạnh nhau mỗi ngày. Không vẽ 2 đường chênh lệch ở đây nữa (chuyển hết về chart 2 để khỏi rối). (4) **Chart 2 (Chênh lệch)**: daily-in-month mode hiển thị 5 đường — 2 đường Chênh lệch từng ngày (TP, GV) + 3 đường lũy kế cộng dồn từ đầu kỳ (Lũy kế TP, Lũy kế GV, Lũy kế Tổng = TP+GV). Mask các điểm tương lai (ngày/tháng > hiện tại) về `null` để không vẽ giả 0. Monthly-in-year mode chỉ hiện đường Lũy kế Tổng cho gọn. (5) **Thẻ chi tiết ngày**: mỗi dòng Giá / Thu / Chi / Chênh có thêm subline `🍖 X · 🧂 Y` bên dưới; dropdown "Chi" hiện danh sách hóa đơn dùng `get_purchase_type_display` (đã tự ra "Mua thực phẩm" / "Mua gia vị") thay cho hardcode "NL chính / Bổ sung". (6) **Excel export Thu-Chi** mở rộng cột: daily mode 11 cột (Giá TP/GV, Thu TP/GV, Chi TP/GV, Chênh TP/GV) thay 4 cột cũ; year mode 8 cột tương tự cho từng tháng (cột "Tổng người ĐK" đổi tên thành "Tổng suất ĐK" cho chính xác — đếm suất ăn chứ không phải đầu người). Dòng TỔNG KẾT có thêm 1 ô cuối "Tổng chênh lệch: X" = Chênh TP + Chênh GV (nền xanh đậm chữ trắng cho nổi). (7) **Excel export Chi phí** month/year mode hiện 4 cột tách (TP - Số phiếu / Tổng chi, GV - Số phiếu / Tổng chi) + cột tổng. Day/week mode đã có cột "Loại mua" sẵn nên giữ nguyên. Loại bỏ `get_meal_price_for_date` khỏi import (chỉ còn nội bộ ở core).
- **Cấu hình giá: lịch tổng quan năm hiển thị cả giá gia vị** [meal_price_list.html](templates/core/meal_price_list.html) + [core/views.py](core/views.py): mỗi ô ngày trong lưới 12 tháng, ngoài giá suất ăn (xanh) còn có dòng `🧂 + giá gia vị` (cam đậm). View đổi `price_map` lưu dict `{meal, spice, food}` thay vì chỉ số; ô tăng `min-height` 42→56px để 3 dòng (số ngày + giá suất + gia vị) không bị chèn.
- **Fix 500 khi tạo/sửa cấu hình giá đè lên khoảng giá cũ** [core/models.py](core/models.py): `MealPriceSetting.clean()` chặn `start_date < today`, nhưng `save()` cũng gọi `full_clean()` — nên khi tạo giá mới overlap với khoảng cũ có `start_date` thuộc quá khứ (vd 06/05–31/05), nhánh truncate gọi `item.save(update_fields=['end_date',...])` cũng kích full_clean() lên bản ghi cũ → ném ValidationError không ai bắt → 500. Sửa: `clean()` chỉ kiểm tra "không được trong quá khứ" khi `start_date` **đang được đặt mới hoặc thay đổi** (so với giá trị trong DB) — bản ghi cũ giữ nguyên start_date (kể cả đã ở quá khứ) vẫn save được. Vẫn chặn được khi user cố sửa start_date sang ngày quá khứ. Đồng thời mở khóa luồng cập nhật meal_price / spice_price / end_date trên các cấu hình giá đã chạy.

### 2026-05-19
- **Tách Thu/Chi/Chênh lệch theo Thực phẩm & Gia vị**: phản ánh cách bếp phân bổ tiền — mỗi suất chia 2 phần: tiền thực phẩm (tôm cá thịt, mua hàng ngày) và tiền gia vị (mắm muối dầu, mua 1 lần dùng nhiều ngày). (1) **Model** [core/models.py](core/models.py): `MealPriceSetting` thêm trường `spice_price` (Giá gia vị mỗi suất, mặc định 0) + property `food_price` = `meal_price − spice_price`; `clean()` chặn gia vị âm hoặc lớn hơn giá suất ăn; `MealPriceChangeLog` thêm `old/new_spice_price`. Migration `core/0006`. (2) **Trang cấu hình giá** [meal_price_form.html](templates/core/meal_price_form.html): nhập 2 ô — Giá suất ăn + Giá gia vị/suất; ô **Giá thực phẩm tự tính** (read-only, JS cập nhật trực tiếp khi gõ). [meal_price_list.html](templates/core/meal_price_list.html): bảng cấu hình thêm 2 cột Giá thực phẩm / Giá gia vị, log đổi giá hiện kèm phần gia vị. (3) **Dashboard** [core/views.py](core/views.py) + [core/dashboard.html](templates/core/dashboard.html): helper mới `get_price_breakdown_for_date` trả `{meal, food, spice}` (`get_meal_price_for_date` giữ nguyên cho module Báo cáo). 3 thẻ KPI Thu / Chi / Chênh lệch đổi thành thẻ chia đôi — mỗi thẻ chia 2 cột 🍖 Thực phẩm và 🧂 Gia vị; Thu = số người × giá tương ứng (có chú thích "N người × giá"), Chi cộng theo phân loại hóa đơn (`main`→thực phẩm, `extra`→gia vị), Chênh lệch = Thu − Chi từng phần. (4) **Biểu đồ tuần**: vẽ lại thành cột chồng — cột Thu (Thực phẩm + Gia vị) và cột Chi (Thực phẩm + Gia vị) cạnh nhau mỗi ngày, kèm 2 đường Chênh lệch Thực phẩm (nét liền) và Gia vị (nét đứt). Trục stacked, mỗi đường có `stack` riêng để không bị cộng dồn.
- **Quy đổi đơn vị nguyên liệu ở trang Phê duyệt** [approval_dashboard.html](templates/meals/approval_dashboard.html): bảng "Nguyên liệu cần chuẩn bị" / "Nguyên liệu mua bổ sung" trước đây hiển thị thô `floatformat:1` (vd `9000,0 g`) — không đổi sang kg/l khi ≥ 1000. Sửa: load `unit_filters`, dùng filter `format_weight` (đã có sẵn, dùng chung với các trang khác) → `9000 g` hiện thành `9 kg`, `ml ≥ 1000` thành `l`, đơn vị khác (quả/củ/cái...) giữ nguyên.
- **Trạng thái "Đăng kí bổ sung" + bữa ăn/bếp ăn mặc định**: (1) Trang Danh sách đăng kí [registration_list.html](templates/registrations/registration_list.html) — dòng `MealRegistration` có `source='supplementary'` hiển thị trạng thái **"Đăng kí bổ sung"** (pill xanh dương `.status-pill-supp`) thay vì "Đặt thành công". (2) Form "Thêm người đăng kí" — ô Bữa ăn / Tên bếp ăn **đề xuất sẵn** Bữa trưa + bếp khu vực 2 (vẫn đổi được). (3) Khi đăng ký bổ sung ở trang Tham gia, bản ghi tạo ra điền sẵn 2 cột bữa ăn / bếp ăn = giá trị mặc định đó. Helper `_default_meal_kitchen` ([registrations/views.py](registrations/views.py)) resolve giá trị mặc định bằng cách dò option đã cấu hình theo từ khóa ('trưa' / 'khu vực 2') — khớp đúng option dù có hậu tố, dùng chung cho form lẫn đăng ký bổ sung. `participation_add_supplementary` đổi `get_or_create` lookup theo `source` → idempotent, không tạo trùng.
- **Đăng ký bổ sung ở trang Tham gia**: xử lý trường hợp người quên đăng ký → lên quét thẻ điểm danh → xin bếp cho ăn + nộp tiền (bếp nấu dư vài suất). Trang Tham gia ([registration_participation.html](templates/registrations/registration_participation.html)) — cột Trạng thái, dòng "Chưa đăng ký" có thêm 2 nút icon song song: **⊕** (đăng ký bổ sung) + **X đỏ** (hủy nếu bấm nhầm). Bấm ⊕ → trạng thái chuyển **"Đã đăng ký bổ sung"** màu xanh dương; bấm X → quay lại "Chưa đăng ký". Lưu dưới dạng `MealRegistration` `source='supplementary'`, 1 suất/người (không migration — `source` là CharField tự do) → **tự được tính vào tổng số suất** nên Thu ở Dashboard + Báo cáo cộng thêm tương ứng. `_build_participation_rows` ([registrations/views.py](registrations/views.py)) đổi trạng thái 'not_registered'→'supplementary' khi có bản ghi bổ sung; thêm label `('Đã đăng ký bổ sung', 'info')`. 2 endpoint AJAX `participation_add_supplementary` / `participation_remove_supplementary` (POST, admin). Excel xuất ([participation_export.py](registrations/participation_export.py)) thêm trạng thái mới: cột Trạng thái hiện "Đã đăng ký bổ sung", tô màu xanh nhạt, dòng tổng có thêm "Đăng ký bổ sung: N". Bộ lọc trạng thái thêm tùy chọn tương ứng.
- **Hiển thị tiền có dấu chấm phân cách nghìn** (1.620.000) ở Đơn giá + Chi phí thực tế: (1) Trang Phê duyệt [approval_dashboard.html](templates/meals/approval_dashboard.html) — đổi 4 chỗ `floatformat:0` → filter `vnd` (đơn giá mặt hàng, chi phí thực tế, tổng). (2) Trang Nhập chi phí [purchase_form.html](templates/finance/purchase_form.html) — ô số `type="number"` không hiển thị được dấu chấm nên đổi ô Đơn giá + Chi phí thực tế sang `type="text" inputmode="numeric"` class `money-input`; JS `formatMoney`/`digitsOnly` định dạng lúc gõ + lúc tải trang, và **bỏ dấu chấm khi submit** để server nhận số sạch. `recalcAiTotals` đọc đơn giá qua `digitsOnly`, đổ tổng vào ô chi phí ở dạng đã định dạng. Widget `actual_cost` trong [DailyPurchaseForm](finance/forms.py) đổi `NumberInput`→`TextInput`. View `purchase_create`/`purchase_update` phòng thủ: bỏ mọi dấu chấm/phẩy trước khi `Decimal()` đơn giá.
- **Bố cục lại chi tiết chi phí theo ngày** [purchase_list.html](templates/finance/purchase_list.html) + [finance/views.py](finance/views.py): trước đây mỗi hóa đơn trong ngày hiện kèm 1 bảng "Dự kiến thực đơn" → trùng lặp, rối. Sửa: "Dự kiến thực đơn" tách thành **1 khối riêng hiện 1 lần cho cả ngày** (view tính `expected_ingredients` 1 lần/ngày thay vì lặp theo từng hóa đơn); danh sách nguyên liệu xếp **lưới ô nhỏ nhiều cột** tự xuống hàng cho gọn, không để bảng full-width trải dài. Mỗi hóa đơn thành 1 thẻ (badge phân loại + chi phí + trạng thái + nút Sửa, ảnh hóa đơn + bảng hóa đơn thực tế), xếp **lưới 2 cột**: 1 đơn → bên trái, 2 → hai bên, 3 → 2 trái 1 phải; mobile về 1 cột.
- **Bỏ ràng buộc chọn đơn mua bổ sung khi nhập hóa đơn "Mua gia vị"** [purchase_form.html](templates/finance/purchase_form.html): trước đây loại `extra` bắt buộc chọn 1 đơn mua bổ sung đã duyệt mới gửi được (ràng buộc nằm ở JS submit). Gỡ ràng buộc — giờ nhập hóa đơn gia vị trực tiếp, không cần chọn nguyên liệu duyệt trước. (Model `extra_request` vốn đã `null=True`, không cần migration.)
- **Đổi tên phân loại mua hàng**: `PURCHASE_TYPE_CHOICES` ([finance/models.py](finance/models.py)) — "Mua nguyên liệu chính" → **"Mua thực phẩm"**, "Mua bổ sung" → **"Mua gia vị"** (giá trị lưu DB `main`/`extra` giữ nguyên, chỉ đổi nhãn hiển thị; migration `0015` AlterField no-op). Sửa các template hardcode nhãn cũ ([purchase_list.html](templates/finance/purchase_list.html), [dashboard.html](templates/core/dashboard.html)) sang dùng `get_purchase_type_display` để sau này đổi tên chỉ 1 chỗ.
- **Bảng mặt hàng hóa đơn: thêm cột "Thành tiền" + tự cộng tổng** [purchase_form.html](templates/finance/purchase_form.html): bảng "Các mặt hàng trong hóa đơn" (chỗ quét AI) thêm cột cuối "Thành tiền" = Số lượng × Đơn giá, tự tính bằng JS. Hàm `recalcAiTotals()`: tính thành tiền từng dòng + cộng tổng đổ vào ô "Chi phí thực tế". Gọi khi: thêm dòng, xóa dòng (`removeAiItemRow`), sửa Số lượng / Đơn giá (event delegation `input` trên tbody), và sau khi quét AI. Lúc tải trang gọi `recalcAiTotals(false)` — chỉ điền cột thành tiền, **không** ghi đè chi phí đã lưu (tránh tự đổi giá trị cũ khi mở form sửa). Bảng `min-width` 540→660px cho cột mới.
- **Nút "Phê duyệt tất cả" cho từng mục ở trang Phê duyệt** [approval_dashboard.html](templates/meals/approval_dashboard.html): mỗi mục (Món ăn / Thực đơn / Đơn mua bổ sung / Chi phí) thêm nút "Phê duyệt tất cả" (xanh, icon `check2-all`) đặt bên trái ô đếm số lượng — chỉ hiện khi mục còn item chờ. Bấm có `confirm()` xác nhận. Backend: 4 view mới `approve_all_dishes/menus/extra_requests/purchases` ([meals/views.py](meals/views.py)) — POST-only, `@user_passes_test(can_manage_approval)`, dùng `.update()` đặt status = approved + approved_by/approved_at cho toàn bộ item pending của loại đó, báo số lượng đã duyệt. Thêm 4 URL `approve_all_*` ([meals/urls.py](meals/urls.py)).
- **Showcase thực đơn: ảnh món to hơn + không bị cắt** [core/dashboard.html](templates/core/dashboard.html): slide từng món trước đây ảnh cao cố định 52% + `object-fit: cover` (cắt ảnh), phần caption thừa nhiều chỗ trống khi mô tả ngắn. Sửa: `.showcase-dish-body` → `flex: 0 0 auto` (chỉ cao bằng nội dung), `.showcase-dish-stage` → `flex: 1` (chiếm hết phần còn lại) nên mô tả ngắn thì ảnh tự to ra; `object-fit` đổi `cover` → `contain` để hiện **đủ ảnh không cắt**; Ken Burns đổi từ phóng 1→1.14 (gây cắt) sang 0.96→1 (luôn ≤ khung nên không cắt); nền khung ảnh đổi sang xám nhạt cho phần lề khi ảnh không phủ kín.
- **Báo cáo Tham gia: gửi NetChat qua DM hoặc channel**: trước đây báo cáo Excel chỉ gửi DM tới từng mã NV. Thêm lựa chọn gửi thẳng vào 1 channel. Modal "Cấu hình gửi báo cáo NetChat" ([registration_participation.html](templates/registrations/registration_participation.html)) thêm radio "Hình thức gửi": **DM tới từng người** (danh sách mã NV như cũ) hoặc **Đăng vào channel** (ô nhập Channel ID) — JS ẩn/hiện phần tương ứng. [participation_export.py](registrations/participation_export.py): thêm config `KEY_SEND_MODE` + `KEY_CHANNEL_ID` (helpers get/set), hàm `send_excel_to_channel` (upload file + post thẳng vào channel, bot phải là thành viên channel), và `send_participation_excel` điều phối theo mode — dùng chung cho cả view gửi tay lẫn management command auto-send. View `participation_settings` lưu/trả thêm `mode` + `channel_id`; `participation_send_netchat` trả `{success, message}` gọn. Command `send_participation_report` cập nhật cổng kiểm tra theo mode. OTP **không** đổi (mã bí mật, vẫn DM).
- **Tối ưu mobile trang Danh sách đăng kí** [registration_list.html](templates/registrations/registration_list.html): trên mobile hàng nút "Import Excel / Thêm người đăng kí / Gửi tin nhắn / Xóa toàn bộ" bị đè lên nhau (flex không xuống dòng). Fix: thêm class `user-list-actions` + `action-btn-group` (kèm `flex-wrap`), thêm media query ≤768px — hàng tổng suất + nút xếp dọc, mỗi nút full-width cao 42px dễ chạm; badge "Tổng suất" canh giữa; giảm padding card filter/list; header bảng thu nhỏ + `white-space: nowrap` cho cuộn ngang gọn; 2 tab trên cùng chia đôi bề ngang. Chỉ sửa CSS + class, không đụng logic.
- **Tối ưu mobile trang Tham gia** [registration_participation.html](templates/registrations/registration_participation.html): trang này trước đây không có media query nào — `filter-grid-participation` (grid 4 cột cố định ~730px) tràn ngang trên mobile. Thêm `@media ≤768px`: filter về grid 2 cột, giảm padding card, header bảng thu nhỏ + `nowrap` cho cuộn ngang, 2 tab chia đôi.
- **Fix cột "Tên hàng" bị bóp trên mobile — trang Nhập chi phí** [purchase_form.html](templates/finance/purchase_form.html): bảng "Các mặt hàng trong hóa đơn" có 4 cột Số lượng/Đơn vị/Đơn giá/Xóa rộng cố định (~340px), cột "Tên hàng" lấy phần còn lại nên trên mobile co còn ~20px (chỉ thấy 2 ký tự). Bảng nằm trong `table-responsive` nhưng thiếu `min-width` nên bị ép co thay vì cuộn. Fix: thêm `min-width: 540px` cho `#ai-items-table` → cột Tên hàng được ~200px, mobile cuộn ngang.
- **Tối ưu ô ngày lịch tháng trang Lên thực đơn** [menu_list.html](templates/meals/menu_list.html): trên mobile lịch 7 cột bị bóp khiến chữ trong ô ("Đã phê duyệt"...) xuống dòng từng từ. Fix: bảng lịch thêm `min-width: 640px` → mobile cuộn ngang, mỗi ô ~90px đọc được; `@media ≤768px` giảm padding ô (`td` + `.menu-cal-day` override `p-3`/`min-height` cũ), thu nhỏ font số ngày + dòng trạng thái. Thêm class `menu-calendar` / `menu-cal-day` để CSS bắt đúng.

### 2026-05-18
- **Dashboard: showcase động cho "Thực đơn ngày hôm nay"**: card thực đơn hôm nay ([core/dashboard.html](templates/core/dashboard.html)) trước đây là list tĩnh, giờ thành màn trình chiếu tự động (phục vụ hiển thị ở bếp ăn). Một slide tổng quan ("Hôm nay có N món" + lưới thumbnail tất cả món) → lần lượt zoom từng món (ảnh lớn hiệu ứng Ken Burns + badge loại món + tên + nội dung giới thiệu hấp dẫn, chữ trượt vào) → hết vòng thì lặp lại. Slide tổng quan 4.5s, mỗi món 5.5s; có chấm chỉ thị ở dưới. Thuần CSS animation + JS nhỏ đổi slide (`setTimeout` đệ quy). CSS `today-menu-*` cũ thay bằng `showcase-*`. **Thêm trường `Dish.description`** ([meals/models.py](meals/models.py), migration `0015_dish_description`): ô "Giới thiệu món ăn" (Textarea) trong form thêm/sửa món ([dish_form.html](templates/meals/dish_form.html) + [DishForm](meals/forms.py)) — admin tự nhập nội dung đặc điểm / nguồn gốc / lợi ích. Showcase hiển thị `description` (lọc `linebreaksbr`, line-clamp 6 dòng) thay cho khẩu phần/nguyên liệu — người ăn không quan tâm thông số kỹ thuật. Món chưa nhập giới thiệu thì slide chỉ hiện ảnh + tên.
- **Thêm người đăng kí: nhập nhiều người cùng lúc**: trang "Thêm người đăng kí" trước đây mỗi lần chỉ lưu 1 người. Đổi sang nhập theo lô — [registration_form.html](templates/registrations/registration_form.html) + view `registration_create` ([registrations/views.py](registrations/views.py)) viết lại: (1) Ngày / Bữa ăn / Bếp ăn / Trạng thái dùng chung cho cả lô, đặt ở trên. (2) Bên dưới là bảng danh sách người — mỗi dòng gồm Mã NV + Họ tên (JS tự điền từ `user_map`) + Số suất, có nút "Thêm dòng" và nút xóa từng dòng (giữ tối thiểu 1 dòng, JS dùng `<template>` clone + event delegation nên dòng thêm sau vẫn auto-fill được). (3) Submit dùng `request.POST.getlist('employee_code'/'quantity')` (cùng pattern `registration_options`). (4) Validate: ngày/bữa/bếp hợp lệ, mã NV tồn tại, số suất ≥ 1, **bắt trùng** mã trong danh sách lẫn trùng với đăng kí đã có trong DB (model có `unique_together`). Nếu bất kỳ dòng nào sai → **không lưu gì cả**, render lại kèm lỗi từng dòng + giữ nguyên dữ liệu đã nhập. Hợp lệ toàn bộ → lưu trong 1 `transaction.atomic()`, báo "Đã thêm N đăng kí". (5) Bấm "Lưu đăng kí" không submit ngay mà hiện popup xác nhận (Bootstrap modal): liệt kê ngày/bữa/bếp dùng chung + danh sách "MNV — Họ tên · số suất" từng người (mã không tồn tại hiện dòng đỏ); bấm "Xác nhận đăng kí" mới gửi thật. JS chặn `submit` event → `e.preventDefault()` → show modal; nút xác nhận gọi `form.submit()` native (bỏ qua listener). Form `MealRegistrationForm` cũ không còn dùng (giữ lại trong [registrations/forms.py](registrations/forms.py), có thể dọn sau).
- **Gọn trang Lên thực đơn — bỏ phần chọn ngày trùng chức năng** [menu_list.html](templates/meals/menu_list.html): xoá card "Xem thực đơn theo lịch" (bộ chọn nhiều ngày + nút "+ Tạo thực đơn" + "Xem thực đơn" + "Đặt lại") và mục "Các ngày đang xem" — vì lịch tháng đã cho bấm vào từng ngày để xem (`focus_date`) hoặc tạo (`menu_create?date=`). Mục "Các ngày đang xem" chỉ nhận dữ liệu từ bộ chọn ngày đó (param `dates`) nên xoá kèm để không thành ô chết. Dọn luôn thư viện flatpickr (CSS CDN + script init) giờ không còn dùng. Trang còn 3 phần: AI gợi ý, Lịch tháng, chi tiết ngày khi bấm. Dọn luôn code chết trong view `menu_list` ([meals/views.py](meals/views.py)): bỏ phần parse param `dates`, dựng list `menus`, nhánh `elif selected_dates` (fallback focus ngày đầu), và 2 key `selected_dates_input` + `menus` trong context.
- **Đồng bộ nút "Xem" toàn hệ thống**: rà soát tất cả template, mọi nút lọc "Xem" giờ dùng chung 1 kiểu — `btn btn-primary btn-sm` + icon phễu `bi-funnel-fill` đứng trước chữ. Sửa 6 nút đang là "Xem" trơn (thiếu icon, vài nút thừa class `px-3`): [review_dashboard.html](templates/reviews/review_dashboard.html) (2 nút), [purchase_list.html](templates/finance/purchase_list.html), [dashboard.html](templates/core/dashboard.html), [meal_price_list.html](templates/core/meal_price_list.html), [menu_list.html](templates/meals/menu_list.html). Nút "Xem thực đơn" ở menu_list cũng thêm icon phễu cho đồng bộ. 2 trang đã đúng từ trước (Báo cáo, Hóa đơn bổ sung) giữ nguyên.
- **Sửa layout trang Cấu hình giá** [meal_price_list.html](templates/core/meal_price_list.html): (1) Xoá hẳn card "Cập nhật giá suất ăn" (card mô tả + nút "+ Thêm giá suất ăn") cho gọn. (2) Nút "+ Thêm giá suất ăn" đổi thành "⚙ Cấu hình giá" (icon `bi-gear-fill`), chuyển vào góc phải card "Tổng quan giá suất ăn năm ..." — nằm cạnh form chọn Năm + nút Xem. (3) Nút "Cấu hình giá" dùng class riêng `.btn-price-config` (gradient indigo→cyan, pill bo 10px, có icon, shadow + hover lift) để khác hẳn nút "Xem" (vốn là `btn-primary btn-sm` nhỏ phẳng).
- **Thêm favicon cho web**: trước đây trình duyệt hiện icon mặc định (quả địa cầu) vì site không có favicon. Thêm 2 thẻ `<link rel="icon">` + `<link rel="apple-touch-icon">` vào `<head>` [base.html](templates/base.html). Ảnh nguồn `static/images/logo.png` (logo chef đầu bếp, 162×175px) hơi chữ nhật nên favicon bị nén ngang nhẹ ~8% — chấp nhận được ở cỡ tab. Favicon dùng bản bo góc `static/images/favicon.png` — sinh từ `logo.png` bằng GraphicsPath rounded-rect (bán kính 28px, 4 góc cắt trong suốt). Query `?v=2` để bust cache trình duyệt. Khi đổi logo: thay `logo.png` rồi tạo lại `favicon.png`.
- **Fix nút "Đăng xuất" bị tàng hình ở sidebar**: nút logout trong [base.html](templates/base.html) dùng class Bootstrap `btn-outline-light` (chữ trắng) — vốn dành cho nền tối, nhưng sidebar đã đổi sang theme sáng pastel nên chữ trắng trên nền sáng gần như không nhìn thấy, phải hover mới hiện. Thay bằng class riêng `.sidebar-logout-btn`: tông hồng rose (`#e11d48`) viền + nền mờ nhạt khớp accent sidebar, hover đổ nền hồng đặc chữ trắng. Hiện rõ ở trạng thái thường.
- **Phóng to mã OTP trong tin nhắn NetChat**: NetChat render Markdown nhưng không cho chỉnh cỡ chữ tùy ý — cách duy nhất làm chữ to là dùng tiêu đề (`#`). Sửa `DEFAULT_OTP` ở [core/message_templates.py](core/message_templates.py): tách `{otp_code}` ra một dòng riêng dạng `# {otp_code}` (tiêu đề H1 → to ~gấp đôi), label và dòng hiệu lực giữ cỡ thường. Lưu ý: mẫu tin nhắn đang chạy được lưu trong DB (`SystemConfig.msg_template_otp`) nên đổi `DEFAULT_OTP` chỉ áp dụng khi DB chưa có mẫu — để áp dụng cho hệ thống thật phải sửa mẫu tại trang Tài khoản → "🔒 Cấu hình tin nhắn gửi OTP".

### 2026-05-15
- **Redesign card "Thực đơn ngày" + "Gửi đánh giá" trang đánh giá** [review_dashboard.html](templates/reviews/review_dashboard.html): (1) Card thực đơn: mỗi món là `.menu-review-item` có border-left 4px màu theo loại món (chính xanh dương / phụ tím / canh xanh lá / tráng miệng cam) + icon emoji 🍚🥬🍲🍮 + pill loại món màu khớp. Nút Like/Dislike refactor từ 2 button outline + count rời sang **vote pill bo tròn** (icon 👍/👎 + số liền nhau), checked-state fill gradient (like xanh emerald / dislike hồng rose) + pop animation + colored shadow. Trạng thái readonly dùng `<span>` thay `<label>`, highlight `.vote-mine`. Header có icon 🍽️ + subtitle "Bấm 👍/👎 để chấm điểm từng món". (2) Card gửi đánh giá: **sửa subtitle sai** "Chạm vào số sao để đánh giá nhanh" (không có sao) → "Để lại góp ý giúp bếp nấu ngon hơn mỗi ngày 🌟". Header icon 💬 (✏️ khi sửa), nền card gradient vàng nhạt. Textarea góp ý bo 12px viền focus vàng amber + label có icon `chat-heart-fill`. Nút gửi gradient indigo→cyan + icon `send-fill` + hover lift. (3) Xoá CSS `:has()` cũ trỏ tới `.btn-outline-success/.empty-icon/.fill-icon` đã không còn tồn tại sau refactor. JS giữ nguyên (`.dish-review-radio` + `data-dish-id` không đổi).
- **Đồng bộ stat cards dashboard với trang báo cáo**: 4 ô KPI ở [core/dashboard.html](templates/core/dashboard.html) (Người ăn / Thu / Chi / Chênh lệch) trước đây dùng `.stat-card` nền gradient đặc kín card, refactor sang style giống trang báo cáo (`.kpi-card`): icon chip 50x50 gradient bên trái (👥/💰/🧾/📊⚠️), body có label uppercase + value 22px font-weight 900 màu theo loại + meta line nhỏ. Background card gradient nhẹ pastel→trắng + border màu khớp, hover lift 2px. Theme: Người ăn xanh dương, Thu xanh lá, Chi hồng, Chênh lệch indigo (dương) / cam (âm). Ô "Chi" giữ clickable (toggle chi tiết) + thêm icon chevron. Value "Người ăn" có unit "suất", các ô tiền có unit "đ". Xoá CSS `.stat-blue/.stat-green/.stat-red/.stat-amber/.stat-card` cũ không còn dùng.
- **Refactor layout trang Phê duyệt**: 2 ô "Món ăn chờ phê duyệt" + "Thực đơn chờ phê duyệt" ở [approval_dashboard.html](templates/meals/approval_dashboard.html) chuyển từ stack dọc thành 2 cột song song (`col-lg-6`) để tận dụng không gian (info ít, list ngắn). Mỗi card có gradient accent bar mảnh ở top (món ăn: cam-vàng-amber; thực đơn: indigo-cyan-emerald) + nền card gradient nhẹ từ pastel xuống trắng làm điểm nhấn. Header có icon chip (egg-fried cho món, journal-bookmark cho thực đơn), count chip màu khớp theme. Item compact mới (`.approval-compact-item`): thumbnail/date-pill 56px bên trái, body ở giữa (title + meta pill + ingredient chips nhỏ), nút action 34x34 icon-only bên phải (✓ xanh gradient, ✗ viền hồng). Date pill thực đơn hiện ngày to đậm + tháng nhỏ trên nền indigo nhạt. Hover item: lift 1px + shadow nhẹ. Empty state có icon check2-circle xám. Responsive: dưới 768px action chuyển sang ngang, thumbnail co còn 48px.
- **Đồng bộ UI cho "Đơn mua bổ sung" + "Chi phí chờ phê duyệt"**: giữ full-width (không chia cột) nhưng áp dụng cùng styling: gradient accent bar top (extra: vàng-cam-hồng; purchase: tím-hồng-đào), icon chip header (cart-plus / receipt-cutoff), count chip màu khớp. Extra request item refactor thành compact-item layout (date pill cam-vàng bên trái, table mặt hàng tighter padding `.compact-table`, action mini icon). Purchase giữ inner structure (bill image + 2-col grid tables vì content phong phú) nhưng đổi action buttons sang mini icon. Nhạt lại accent bars: opacity 0.7, dùng tones lighter (rose-200/amber-200/emerald-200 thay vì rose-400/amber-500/emerald-500), height 3px thay vì 4px — UX nhẹ nhàng hơn, không ăn vào sự chú ý của content.
- **Fix modal từ chối hiển thị sai prefix**: trước đây dù từ chối món ăn / thực đơn / chi phí đều hiện "Nhập lý do từ chối ngày ...". Fix: thêm `<span id="rejectPrefix">` và JS set theo `data-type` — `"món ăn"` → "món", còn lại → "ngày". Modal giờ hiện đúng "Nhập lý do từ chối **món** Thịt kho tàu" cho dish và "Nhập lý do từ chối **ngày** 15/05/2026" cho menu/purchase.
- **Gộp filter vào card danh sách hóa đơn**: trang [extra_request_list.html](templates/finance/extra_request_list.html) trước đây có 2 card stack dọc (1 card filter + 1 card list). Gộp thành 1 card duy nhất: title "Danh sách hóa đơn ngày DD/MM/YYYY" bên trái, form filter inline (`.order-filter-inline`) ở góc phải header. Form filter thu gọn: label uppercase 11px màu xám, input/select height 34px width tối thiểu 120px (140px riêng ô date), nút "Xem" có icon funnel, "Reset" giữ text. Border bottom mảnh phân tách header với table. Responsive < 992px: filter wrap xuống nhiều dòng, actions chiếm full width.
- **Fix bug import + cân đối table đơn bổ sung**: (1) [finance/views.py](finance/views.py) thiếu import `ExtraPurchaseRequestItem` → NameError khi POST tạo đơn bổ sung, đã thêm vào dòng import. (2) Bảng items trong card "Đơn mua bổ sung chờ phê duyệt": bỏ cột "Đơn giá" (admin không cần xem ở bước phê duyệt). Refactor từ `<table>` sang CSS Grid (`.extra-items-list`) với 3 cột cố định pixel: Nguyên liệu `1fr` / Số lượng `110px` (text-end, `tabular-nums`) / Đơn vị `90px` (text-start) — header + rows dùng cùng grid template nên 3 cột luôn thẳng cột 100%, không còn lệch khi qua các table khác nhau.
- **Audit log + badge cho chi phí đã chỉnh sửa sau duyệt** (BLOCKER tính minh bạch): Trước đây admin có thể sửa chi phí đã được duyệt → status reset về pending → re-approve → nhưng không có dấu vết, mất tính công khai. Bổ sung: (1) Field `was_edited_after_approval` (BooleanField) trên [DailyPurchase](finance/models.py) — flag vĩnh viễn, kể cả sau khi duyệt lại vẫn giữ badge. (2) Model mới `PurchaseEditLog` (FK purchase, edited_by, edited_at, previous_status, reason) — mỗi lần sửa chi phí đã-duyệt là tạo 1 log. (3) [purchase_update view](finance/views.py): nếu previous status là APPROVED → bắt buộc nhập field `edit_reason` (POST), nếu thiếu thì render lại form kèm error, không cho save; khi save thành công thì set flag + tạo log. Sửa khi pending/rejected không bắt buộc lý do (chỉ sửa khi đã duyệt mới có dấu vết). (4) [purchase_form.html](templates/finance/purchase_form.html): khi edit chi phí đã duyệt hiện box cảnh báo gradient cam (icon ⚠ + text "Mọi chỉnh sửa sẽ được ghi lại") + textarea "Lý do chỉnh sửa *" required. (5) [purchase_list.html](templates/finance/purchase_list.html): cột Trạng thái có thêm badge "✏️ Đã chỉnh sửa" gradient cam (clickable mở Bootstrap modal hiển thị toàn bộ edit_logs — mỗi log có time + user + lý do, design card gradient cam-trắng matching theme). Modal đặt ngoài `<tbody>` cho HTML hợp lệ. (6) [approval_dashboard.html](templates/meals/approval_dashboard.html): khi admin review chi phí pending, nếu nó từng-là-approved-rồi-sửa thì hiện badge cam cùng tooltip + `<details>` collapsible với list logs — giúp admin biết "cẩn thận, item này không phải mới mà là sửa lại". Migration: `0014_dailypurchase_was_edited_after_approval_and_more`.
- **UI tweak danh mục món ăn**: Filter form ở [dish_list.html](templates/meals/dish_list.html) (search + dropdown loại + lọc + reset) trước đây stack riêng dưới header, giờ chuyển sang inline cùng hàng với "Thêm món ăn" — gọn hơn nhiều, theo pattern `.dish-filter-inline` (input/select 34px height bo 9px, search box placeholder có icon 🔍, button "Thêm món ăn" có icon `bi-plus-lg`). Border-bottom mảnh phân tách header với table. Responsive < 767px filter wrap full-width.
- **Redesign trang Báo cáo** [report_dashboard.html](templates/reports/report_dashboard.html): (1) Header thống nhất kiểu mới: title có icon emoji (📈/📉/📅/📥) + subtitle có `bi-calendar-range` icon, form filter inline ở góc phải (`.report-filter-inline` reuse từ trang hóa đơn — label uppercase 11px xám, input/select height 34px), button "Xem" có icon funnel. Border-bottom mảnh phân tách header với content. (2) Stat cards Thu/Chi/Chênh lệch: refactor từ stat-card cũ thành 3 card mới với icon chip 48x48 gradient (💰 xanh / 🧾 hồng / 📊⚠️ indigo-cam tuỳ dấu), background gradient nhẹ theo trạng thái, value 22px font-weight 900 + unit "đ" mờ, hover lift 2px. (3) Day grid (Chi tiết các ngày): card mới `.report-day-card` có gradient bg + accent bar top theo trạng thái (dương: indigo→cyan, âm: vàng→cam, zero: emerald, rỗng: trắng mờ), header có ngày + trend arrow chip (↑ indigo / ↓ cam / = emerald), rows Thu/Chi/Chênh lệch màu theo nghĩa, dòng "Chi" clickable bo góc hover, detail box thu gọn ~11.5px. Width responsive 7 cột → 6 → 4 → 3 → 2. (4) Export cards: 3 card có accent bar gradient trên top (blue/green/purple), background gradient nhẹ matching theme, icon emoji 📊/🧾/⭐, hover lift 3px + shadow, button gradient solid với hover lift. (5) **Chart "Biểu đồ chênh lệch Thu - Chi" đổi từ bar sang line cong** tension 0.4 + area fill gradient indigo nhạt + points trắng viền theo dấu (indigo nếu ≥0, cam nếu <0), segment.borderColor động theo dấu của điểm tiếp theo (đường nối giữa âm/dương đổi màu). (6) Chart 3 cột Thu/Chi/Chênh lệch giữ type bar nhưng áp gradient dọc (emerald/rose/indigo), borderRadius 8, tooltip nền đen mờ + format VNĐ chuẩn, legend point-style circle top-end, y-axis ticks "1.2tr"/"500k". Height chart 280px (chart 1) và 320px (chart 2).
- **Redesign card "Thực đơn trong tuần" + chart "Xu hướng trong tuần"** trên dashboard: (1) Day card có pseudo-element `::before` 3px gradient bar trên đỉnh theo trạng thái — selected: indigo→cyan, có-menu: xanh nhạt, empty: trắng mờ. Card hover lift 3px + border indigo-200, transition cubic-bezier mượt hơn. Selected card có gradient bg indigo nhạt→trắng→cyan thay vì xanh đơn cũ. (2) Menu group items (món chính / phụ / canh / tráng miệng) refactor: thêm icon emoji riêng (🍚 chính / 🥬 phụ / 🍲 canh / 🍮 tráng miệng) + gradient bg theo loại (xanh dương / tím / xanh lá / cam — chuyển từ trắng sang trắng mờ), text màu khớp theme (chữ đậm hơn), bo 10px, hover dịch phải 2px. List bullet bỏ (`list-style: none`), font 13px weight 600. Empty state có icon 🌱 thay text mờ. Footer "Đang xem" có icon `bi-eye-fill` + màu indigo. (3) Chart Chart.js redesign: Thu/Chi giờ là **bar gradient dọc** (emerald top→trong suốt bottom / rose top→trong suốt bottom) bo 8px; Chênh lệch chuyển từ bar sang **line cong tension 0.4** + area fill gradient indigo nhạt, có points trắng viền indigo. Tooltip nền đen mờ bo 10px hiện cả 3 series + format VNĐ `Intl.NumberFormat('vi-VN')`. Legend chuyển point-style circle, position top-end, padding rộng hơn. Y-axis ticks format thông minh: ≥1tr hiện "1.2tr", ≥1k hiện "500k". X-axis bỏ grid line. Height chart 220px → 260px. Card có gradient bg trắng→xanh-trắng và title icon 📊.
- **Redesign box AI dinh dưỡng ở dashboard**: trang [core/dashboard.html](templates/core/dashboard.html) ô "AI dinh dưỡng" được làm bắt mắt sinh động hơn. (1) Background: gradient lavender→trắng→cyan + 2 decorative blobs (tím + cyan) blur động "trôi" với animation 8-10s + 2 sparkle ✨ nhấp nháy. (2) Header: avatar 46x46 gradient indigo→cyan với pulse ring (vòng tròn phồng ra mờ dần) + bot emoji 🤖 bob nhẹ, title gradient text indigo→cyan, status pill "● Đang hoạt động" có dot xanh emerald nhấp nháy radar-style. (3) Loading state: bot bobbing + 3 chấm typing animation kiểu chat app thật. (4) Card phân tích: nền trắng mờ + shadow indigo nhạt, kcal hiển thị **36px gradient indigo→cyan→emerald** thay vì xanh đơn (số to bắt mắt), badge "Mức đánh giá" pill có màu theo level — `cân bằng`→emerald + ⚖️, `thiếu/thấp`→cam + ⬇️, `dư/cao/nhiều`→hồng + ⬆️ (JS detect từ string). (5) Card "Thông tin bổ ích": gradient mint→vàng nhạt, icon 💡 có animation glow vàng. Tất cả animation nhẹ nhàng (2-2.5s loop), không gây mất tập trung — vẫn giữ tone công sở.

### 2026-05-14
- **Sidebar đổi sang theme sáng pastel**: gradient 4 lớp (hồng → lavender → xanh → mint) trong [base.html](templates/base.html), thay cho dark slate cũ. Brand box thành card trắng mờ + backdrop-blur; brand title gradient hồng→indigo→cyan. Menu item: text slate-600, icon xám nhạt, hover nền trắng + icon đổi tím + dịch nhẹ 3px. Active item: gradient hồng-tím pastel + viền tím nhạt + chấm tròn hồng có halo bên phải. Shadow mềm hơn, border rất nhạt — UX nhẹ nhàng tối giản, không màu mè.
- **Topbar refresh**: thêm thanh accent dọc 3px (gradient cyan→emerald) bên trái title, title font 22px letter-spacing chặt, subtitle có chấm xanh nhỏ. Góc phải: "Xin chào, **TÊN VIẾT HOA**" (cyan đậm) + "Thứ X, DD/MM/YYYY" (xám nhạt, JS tự render theo giờ máy). Tên lấy từ `UserProfile.full_name`, fallback `username`. Mobile ẩn block phải để gọn.
- **Đề xuất món ăn mới** trên trang đánh giá: thêm "happy zone" pastel (gradient xanh non + hồng nhạt + tím) trong card "Gửi đánh giá của bạn" ở [review_dashboard.html](templates/reviews/review_dashboard.html). User gõ tên món + Enter/click → upsert vào model mới [DishSuggestion](reviews/models.py) (`name`, `name_normalized` unique để dedupe, `count`, `last_voted_by`). **1 user chỉ vote 1 lần/món** — tracked qua model phụ `DishSuggestionVote(suggestion, user)` với `unique_together`. Tag user đã vote có viền xanh emerald + dấu ✓, click vào hiện toast "Bạn đã vote rồi". Hiển thị floating tag bo tròn, **size tăng theo độ phổ biến** (size-2 từ 2 vote, size-3 từ 50% max, size-4 + animation pulse từ 75% max). Throttle 2s/click chống spam. Endpoint mới `/reviews/dish-suggestions/` (GET trả top 30 + `voted_by_me` per item, POST upsert + tạo vote record, dùng `transaction.atomic` + `IntegrityError` để handle duplicate vote). Animation emoji 🍜 bounce + tag pop khi xuất hiện. Migrations `0006_dishsuggestion` + `0007_alter_dishsuggestion_count_dishsuggestionvote`.
- **Fix bug đơn vị nguyên liệu ở "Khối lượng cần chuẩn bị"**: filter [format_weight](meals/templatetags/unit_filters.py) trước đây luôn output `g`/`kg` bất kể đơn vị thực — vd nguyên liệu đơn vị `quả` thì tổng vẫn hiển thị "7,90 g" (sai). Fix: filter giờ nhận argument `unit` (`{{ value|format_weight:item.unit }}`). Mặc định `g` (giữ tương thích); `ml` tự đổi sang `l` khi ≥1000; đơn vị khác (`quả`, `củ`, `cái`...) giữ nguyên + format VN số. Số nguyên không hiển thị phần thập phân thừa (`7 quả` thay vì `7,00 quả`). Sửa [menu_list.html](templates/meals/menu_list.html) pass `item.unit`.
- **Xuất Excel báo cáo Tham gia hàng ngày**: thêm icon Excel (xanh lá) ở header cột "Loại" của trang [registration_participation](templates/registrations/registration_participation.html). Click → tải file `tham_gia_YYYY-MM-DD.xlsx` chứa cả 3 trạng thái (Đã điểm danh / Chưa điểm danh / Chưa đăng ký), có tiêu đề, header xanh, 3 nhóm tô nền màu khác nhau, summary cuối bảng. Logic build Excel tách ra module [registrations/participation_export.py](registrations/participation_export.py) (`build_excel_bytes`) để dùng chung với feature gửi NetChat.
- **Gửi báo cáo Tham gia qua NetChat DM**: thêm icon máy bay (xanh dương) cạnh icon Excel. Click → confirm → upload file Excel qua Mattermost `POST /api/v4/files` rồi tạo post với `file_ids` vào DM của từng MNV trong danh sách cấu hình. Trả về JSON `{sent_count, failed: [{code, reason}]}` để toast hiện kết quả. Helper `send_excel_to_recipients` trong [registrations/participation_export.py](registrations/participation_export.py).
- **Modal "Cấu hình gửi báo cáo NetChat"**: icon bánh răng (xám) cạnh icon gửi. Mỗi MNV là 1 row riêng (ô input nhỏ 140px + tên auto-fill bên phải + nút xoá). Gõ MNV match DB → viền xanh + tên đậm xanh; không match → viền đỏ + "⚠ Không tồn tại". Có nút "Thêm người nhận" để thêm row mới. Counter chỉ đếm MNV hợp lệ. Lưu vào `SystemConfig` keys `participation_export_recipients` (JSON list) + `participation_export_send_time` (HH:MM, hiện chưa kích hoạt cron). Endpoints `/registrations/participation/settings/` (GET/POST JSON) + `/registrations/participation/send-netchat/` (POST).
- **Resume countdown nút "Gửi tin nhắn báo cơm" qua reload trang**: trước đây countdown chỉ ở JS memory → đổi tab/F5 là mất, user tưởng đã xong nên bấm lại. Fix: khi POST `send_meal_notifications`, ghi `notification_job_active_until` (ISO timestamp) + `notification_job_total` (N) vào SystemConfig **trước** khi start thread. View `registration_list` đọc lại, nếu `active_until > now` → pass `notification_remaining_seconds` + `notification_total_count` xuống template, JS auto gọi `startSendingCountdown(remaining, total)` khi load. Khi thread `_send_notifications_bg` xong → clear `active_until`. Fallback: nếu thread crash giữa chừng, state vẫn auto-expire theo thời gian.
- **Auto-gửi báo cáo Tham gia qua NetChat hàng ngày**: thêm management command [send_participation_report](registrations/management/commands/send_participation_report.py) + systemd timer [participation-report.timer](scripts/participation-report.timer) chạy mỗi 1 phút. Command check giờ:phút VN (qua `timezone.localtime()`, server clock UTC nhưng Django settings `TIME_ZONE='Asia/Ho_Chi_Minh'` tự convert) khớp `send_time` cấu hình + idempotent guard `participation_export_last_sent_date` → chỉ gửi 1 lần/ngày. Đa số lần fire (1440/ngày) là no-op ~1s. Manual button vẫn chạy song song độc lập. Cờ `--force` cho phép admin test thủ công bỏ qua check thời gian. Setup + debug instructions ở [DEPLOY.md](DEPLOY.md) section `participation-report.timer`.
- **Fix bug form "Thêm người đăng kí"**: (1) Auto-fill họ tên sai do double-encode JSON (pass `json.dumps(dict)` qua `|json_script` filter — filter tự encode 1 lần, không cần dumps trước). (2) Đổi UI cấu hình options từ textarea sang chip pattern (input + nút Thêm + chip với X xoá), gọn hơn. (3) Status field viền/nền theo state: trống = không màu, sai = đỏ, đúng = xanh nhạt; dropdown chưa chọn = đỏ luôn. (4) Bỏ duplicate alert "Đã lưu" trong template options (base.html đã render rồi). (5) Bấm "Lưu cấu hình" hoặc "Huỷ" đều redirect về form đăng kí.

### 2026-05-13
- **Bảo mật**: tách `SECRET_KEY`, `DEBUG`, `ALLOWED_HOSTS` ra env var (TODO #3 + #4). [config/settings.py](config/settings.py) giờ đọc từ `SECRET_KEY` / `DJANGO_DEBUG` / `DJANGO_ALLOWED_HOSTS`. Default an toàn cho prod: `DEBUG=False`, `ALLOWED_HOSTS=[]` (Django sẽ chặn nếu chưa set env). Dev không set env thì DEBUG=False mặc định; muốn xem trace phải `DJANGO_DEBUG=True` trong `.env`. Khi DEBUG=True và `DJANGO_ALLOWED_HOSTS` rỗng, fallback `localhost,127.0.0.1` để dev khỏi 400. Trên prod cần set 3 env mới trong systemd service.
- [.env.example](.env.example): thêm 3 biến mới `SECRET_KEY`, `DJANGO_DEBUG`, `DJANGO_ALLOWED_HOSTS` với hướng dẫn generate key + ví dụ giá trị prod.
- [config/settings.py](config/settings.py): commit `CSRF_TRUSTED_ORIGINS` vào repo (trước đây là chỉnh tay trực tiếp trên server, gây conflict khi pull). Giá trị giống y bản server đang chạy: `https://net2kitchen.viettel.pro.vn` + `http://...`.
- **Đã deploy + verify trên prod**: service `bep-an` chạy OK sau khi set `SECRET_KEY` + `DJANGO_DEBUG=False` + `DJANGO_ALLOWED_HOSTS` trong systemd. Verify: (1) URL sai → trang 404 ngắn gọn không lộ stack trace; (2) request qua IP (sai host) → 400 Bad Request. Session user cũ bị invalidate do `SECRET_KEY` đổi (expected).
- [DEPLOY.md](DEPLOY.md): cập nhật danh sách env var trên server (thêm 3 biến mới) + ví dụ block `[Service]` đầy đủ.
- [TODO.md](TODO.md): đóng mục #3 và #4 bảo mật. Nhóm bảo mật còn lại 2 mục: #1 (đổi DB password), #2 (revoke Gemini API key — đặc biệt cần vì key vừa lộ thêm 1 lần trong session deploy hôm nay).
- **Bảo mật cookies**: thêm `SESSION_COOKIE_SECURE`, `CSRF_COOKIE_SECURE` (auto theo DEBUG), `*_HTTPONLY=True`, `*_SAMESITE='Lax'` và `SECURE_PROXY_SSL_HEADER` vào [config/settings.py](config/settings.py). Chặn XSS steal cookie + đảm bảo cookie chỉ gửi qua HTTPS trên prod. Đã verify Nginx Proxy Manager (container Docker terminate HTTPS) có gửi header `X-Forwarded-Proto` → Django nhận biết HTTPS đúng cách. Đã deploy + verify trên prod: cookie `sessionid` và `csrftoken` đều có flag Secure + HttpOnly + SameSite=Lax. Fix BLOCKER #2 trong audit prod-readiness 2026-05-13.
- **OTP brute-force protection + UX "Gửi lại mã"** (fix BLOCKER #1 audit): thêm 3 field vào [UserProfile](accounts/models.py) (`otp_failed_attempts`, `otp_locked_until`, `otp_last_sent_at`) + 4 helper method. Quy tắc: (1) verify sai 10 lần → khoá 15 phút; (2) khi đang khoá, request/resend OTP đều KHÔNG gửi tin NetChat mới (tránh spam) — chỉ redirect tới trang verify để user thấy countdown mở khoá; (3) min 60s giữa 2 lần request/resend; (4) verify thành công → reset attempts. Refactor logic gửi OTP ra helper `_send_otp_netchat()` dùng chung cho request và resend. Thêm view + URL [resend_otp](accounts/views.py) (`/accounts/resend-otp/`). UI: [otp_verify.html](templates/registration/otp_verify.html) thêm nút "Gửi lại mã" với countdown 60s, banner lock với countdown đến giờ mở khoá (auto reload page khi hết khoá), thông báo "còn N lần thử" khi sai mã. Migration mới `0008_userprofile_otp_failed_attempts_and_more`.
- **UX "Gửi tin nhắn báo cơm"**: nút giữ disable + spinner "Đang gửi (N người) - còn M:SS" trong suốt thời gian background thread chạy (ước tính `ceil(N/15) * 75s + 15s buffer` theo throttle 15 tin/phút của [_send_notifications_bg](registrations/views.py)). Tránh user spam-click queue thêm batch trùng. Khi hết countdown → tự enable lại. [registrations/registration_list.html](templates/registrations/registration_list.html) refactor JS handler.
- **Format ngày trong tin báo cơm**: ngày trong message giờ hiển thị `13-05-2026` thay vì `2026-05-13`. Sửa ở [_send_notifications_bg](registrations/views.py) — parse target_date ISO từ frontend rồi format `%d-%m-%Y` trước khi pass vào `render_template`. Template `{target_date}` vẫn giữ nguyên (không cần đổi cấu hình SystemConfig).
- **Thêm biến `{meal_count}` cho tin báo cơm**: hiển thị số lượng suất ăn (lấy từ `MealRegistration.quantity`), zero-pad 2 chữ số (`01`, `02`, … `12`). Cập nhật [core/message_templates.py](core/message_templates.py): thêm vào `VARS_MEAL` (xuất hiện trong UI cấu hình Profile admin) + sửa `DEFAULT_MEAL`. Sửa [_send_notifications_bg](registrations/views.py) pass thêm `meal_count` vào render_template. ⚠️ Template hiện tại của admin nằm trong DB (`SystemConfig`) sẽ KHÔNG tự đổi — admin cần vào trang Profile để chèn `{meal_count}` vào template hiện có.
- **Backup PostgreSQL daily** (fix audit BLOCKER #3): thêm 3 file vào [scripts/](scripts/): [db_backup.sh](scripts/db_backup.sh) (pg_dump + gzip + retention 7 bản), [db-backup.service](scripts/db-backup.service) (systemd oneshot), [db-backup.timer](scripts/db-backup.timer) (chạy 19:00 UTC = 02:00 VN). Backup lưu tại `/home/ipcdkv2/db_backups/`, owner ipcdkv2 mode 600 (chứa hash mật khẩu nên chỉ owner đọc). Hướng dẫn setup lần đầu + recovery + debug ở [DEPLOY.md](DEPLOY.md) section `db-backup.timer`. Đã verify restore với DB tạm — 320 user profile khôi phục đầy đủ kèm tables/indexes/FK.
- **UX form "Thêm người đăng kí"**: (1) `meal_name` + `kitchen_name` đổi từ text input sang `<select>` dropdown, options đọc từ SystemConfig (default seed: 3 bữa + 3 bếp Viettel). (2) Nhập MNV → tự fetch `full_name` từ DB (embed dict UserProfile vào template qua `json_script`), ô họ tên readonly + mờ. MNV không tồn tại → báo inline "vui lòng tạo tài khoản trước", form chặn submit (validation server-side ở `clean_employee_code`). (3) Trạng thái fix cứng `"Đặt thành công"` + readonly + backend force qua `clean_status` (không tin client). (4) Icon bánh răng góc phải form → trang `/registrations/options/` để admin/kitchen cấu hình danh sách dropdown. Tạo module mới [registrations/options.py](registrations/options.py) (key SystemConfig `registration_meal_options` / `registration_kitchen_options`, JSON list-of-strings, khử trùng giữ thứ tự). Template mới [registration_options.html](templates/registrations/registration_options.html) với 2 textarea (1 dòng = 1 option). Sửa [forms.py](registrations/forms.py), [views.py](registrations/views.py), [urls.py](registrations/urls.py), [registration_form.html](templates/registrations/registration_form.html).

### 2026-05-12
- Trang Login mobile: thêm "mobile-brand" banner trên cùng (🍱 + "Net2Kitchen" + subtitle) chỉ hiện ở màn hình ≤900px — fix vấn đề user mở trang trên điện thoại không biết đang đăng nhập phần mềm gì (panel trái với ảnh nền bị ẩn trên mobile). Đồng bộ branding panel trái sang "Net2Kitchen" / "© Ban CĐS – TTKTKV2".
- Sidebar branding: đổi "Hệ thống quản lí bếp ăn" → **"Net2Kitchen"** (text gradient cyan→green), subtitle "@ CĐS Team - KV2" → "© Ban CĐS – TTKTKV2". Refactor brand-box thành layout flex (emoji + text) cho gọn; cập nhật CSS sidebar collapsed mode để show đúng emoji thật thay vì `::before`.
- DEPLOY.md: nâng bước "Apply migration" thành quy tắc bắt buộc chạy MỖI lần deploy (kể cả khi nghĩ schema không đổi) — sau sự cố production lỗi `ProgrammingError: column meals_weeklymenudraft.status does not exist` do migration commit trước chưa apply trên server.
- **Bắt buộc duyệt với mọi role (kể cả admin)** để double-check tránh sai sót. Trước đây: admin tạo `Dish` auto-approve ngay; admin tạo/sửa `DailyMenu` có thể chọn `STATUS_APPROVED` thẳng trong dropdown form → bypass bước duyệt. Sau fix: (1) `dish_create`/`dish_update` luôn set status=PENDING bất kể role; (2) `DailyMenuForm` disable field `status` cho mọi role, `menu_create`/`menu_update` luôn force PENDING. Admin muốn duyệt phải dùng action "Phê duyệt" tách riêng. Đơn mua bổ sung và chi phí mua đã PENDING cho mọi role từ trước — không cần đổi.
- Fix bug menu bị từ chối: khi user (đặc biệt là admin) chỉnh sửa menu đã REJECTED và bấm "Cập nhật thực đơn", status vẫn giữ `rejected` (ô lịch vẫn đỏ). Nguyên nhân: với admin, code đọc `form.cleaned_data['status']` mà form initial inherit từ instance = rejected → không change → save lại rejected. Sửa: bắt `was_rejected` ngay đầu view trước khi form __init__, nếu trước đó là REJECTED thì luôn force PENDING khi resubmit (bất kể role). Áp dụng cho cả `menu_create` và `menu_update`.
- Refactor workflow "Gợi ý AI tuần sau": tách 2 trạng thái rõ ràng cho `WeeklyMenuDraft` (migration 0014). (1) **`suggested`** — sau khi bấm "AI gợi ý", chỉ hiện preview cards, lịch tháng vẫn trắng. (2) **`applied`** — sau khi bấm "Áp dụng gợi ý", lịch tháng đổi xanh dương, day detail pre-tick các món, KHÔNG tạo `DailyMenu` ngay. (3) User vào day detail tinh chỉnh + bấm "Lưu thực đơn" → mới tạo `DailyMenu` PENDING (xanh lá, chờ phê duyệt), draft tự xóa. (4) Admin duyệt → APPROVED. Fix kèm bug `name 'json' is not defined` trong `apply_week_menu_draft` do thiếu `import json`.
- UI nút "Gợi ý thực đơn" đổi thành "AI gợi ý" với pill viền gradient cyan→green→amber + icon `bi-stars`, nền trắng — phù hợp với phong cách "AI button" hiện đại.
- Trang "Lên thực đơn": gợi ý AI tuần sau giờ persist qua reload trang. Khi `WeeklyMenuDraft` được tạo trong ngày hôm nay vẫn nằm DB, view load lại + đẩy xuống template, JS render preview giống lúc bấm "Gợi ý". Bấm "Gợi ý thực đơn" sẽ overwrite (xóa draft cũ + tạo mới — logic này đã có sẵn). Sau nửa đêm filter `created_at__date=today` không match → tự động không hiển thị. Tiết kiệm quota AI Gemini.
- Cập nhật `AVAILABLE_MODELS` trong [core/ai_config.py](core/ai_config.py): bỏ các model đã deprecated (1.5-pro, 1.5-flash, 2.0-flash, 2.0-flash-exp), thêm model hiện hành theo AI Studio (3.1-pro, 3.1-flash-lite, 3-flash, 2.5-pro, 2.5-flash, 2.5-flash-lite). Lưu ý: `*-pro` cần tài khoản trả phí — free tier quota = 0.
- Trang Tham gia: dịch nhãn trạng thái sang tiếng Việt (`valid → Đã điểm danh`, `not_registered → Chưa đăng ký`) và **bổ sung nhóm "Chưa điểm danh"** (đỏ) — những người có trong `MealRegistration` ngày đó nhưng chưa có lần quét nào trong `AttendanceLog`. Filter trạng thái thêm option mới `not_attended`. View refactor sang dùng `rows` (struct thống nhất từ 2 nguồn) thay vì `logs` (chỉ AttendanceLog).
- UI dọn topbar: xóa badge "Nội bộ" góc phải (không có ý nghĩa thông tin), giảm `content-wrap` padding-top từ 28px → 12px để nội dung sát topbar hơn, đỡ khoảng trống thừa ở đầu mỗi trang.
- Fix ảnh static không hiển thị trên server (login background, default avatar): thêm URL pattern serve `/static/` qua `django.views.static.serve` trong [config/urls.py](config/urls.py) (mirror cách media đang serve). Lý do: gunicorn không tự serve static như `runserver` ở dev, kể cả khi `DEBUG=True`. Thêm file `static/images/default-avatar.png` (trước đây lạc trong `media/user_avatars/`).
- Fix `attendance_log_api`: nếu cùng ngày & cùng `employee_code` đã có record nhưng **trạng thái khác** (vd quét lần 1 `not_registered`, đăng ký bù xong quét lần 2 `valid`) → cập nhật record cũ (status + scan_time + full_name) thay vì bỏ qua. Cùng trạng thái vẫn skip để giữ dedup. Trả thêm field `updated` trong response.
- Tăng gunicorn `--workers 3 → 5`, thêm `--timeout 120` (mặc định 30s không đủ cho Gemini AI call). Fix bug: worker đang gọi AI menu suggestion bị master kill SIGKILL sau 30s → connection của request khác đứng cùng worker bị reset → người dùng thấy "Internal Server Error", F5 lại OK.
- **Gỡ bỏ tính năng thu thập góp ý qua DM NetChat** (`FeedbackMessage` / `poll_feedback`). Lý do: bot bị NetChat soft-suspend lúc 9:30 do `poll_feedback` gọi 44+ API call/phút sau khi bot tự broadcast tin đặt cơm. Phương án sửa root-cause (per-channel cursor / webhook) phức tạp; user quyết định tắt hẳn, bắt buộc góp ý qua web. Xóa: `reviews/feedback_poller.py`, `reviews/management/commands/poll_feedback.py`, model `FeedbackMessage` (migration `0005_delete_feedbackmessage`), cột "Đánh giá từ NetChat" trong trang đánh giá. Trên server: dừng + disable + xóa `poll-feedback.timer/.service` (xem [DEPLOY.md](DEPLOY.md)).
- (Sửa trước khi quyết tắt) Throttle `poll_feedback` lên 1.2s/call và abort khi 429 — code này đã được xóa cùng feature.

### 2026-05-11
- Dashboard: ẩn badge "Đã đăng kí / Chưa đăng kí" ở các thẻ ngày trong tuần khi đăng nhập bằng tài khoản superuser (admin Django) — dùng cho màn hình TV ở bếp ăn (tài khoản này không bao giờ đăng ký ăn).
- Trang Tham gia (`registration_participation`): cột "Người dùng" hiển thị tên thực (lookup `UserProfile` theo `employee_code`, fallback `log.full_name` rồi đến mã NV) kèm avatar; header cột thêm badge "Tổng: N" (đếm distinct nhân viên sau filter); filter Trạng thái đổi label/value sang `valid` / `not_registered` cho khớp dữ liệu API; ô tìm kiếm giờ match cả tên và mã NV.
- Thu thập tin nhắn góp ý DM bot NetChat: model `FeedbackMessage`, polling service + management command `poll_feedback` (cron 30 phút qua systemd timer), hiển thị tích hợp ngay trong section "Tổng hợp đánh giá" của trang đánh giá (chia 2 cột website / NetChat). Tin cùng người cùng ngày được gộp 1 dòng cách nhau bằng " / " (dùng `StringAgg` PostgreSQL).
- UI tinh chỉnh: trang Danh sách đăng kí gộp filter + Log gửi tin trên cùng 1 hàng (9/3); trang Đánh giá hiển thị compact `MNV - Tên: nội dung` 1 dòng, MNV màu xanh.
- Fix: template `{{` đầu dòng → raw text; `});` rác cuối menu_list; crash `VariableDoesNotExist` khi `profile=None` (đăng ký có mã NV chưa có user).
- Tạo [DEPLOY.md](DEPLOY.md) — quy trình deploy chuẩn, troubleshooting, danh sách env var trên server.

### 2026-05-10
- Thu thập góp ý qua tin nhắn DM NetChat: thêm model `FeedbackMessage` + service [reviews/feedback_poller.py](reviews/feedback_poller.py) + management command `poll_feedback`. Trang `/reviews/feedback/` hiển thị bảng tổng hợp (filter theo ngày, search), link từ trang đánh giá. Cần setup systemd timer trên server để chạy định kỳ — xem [TODO.md](TODO.md) hoặc hướng dẫn deploy.
- Tách `DB_PASSWORD` ra biến môi trường (`python-dotenv` + file `.env` gitignored). Deploy giờ chỉ cần `git pull + restart`, không còn conflict do secrets khác nhau giữa dev và prod.
- Cấu hình runtime trong Profile admin: BOT NetChat, AI Gemini (model + API key), mẫu tin nhắn OTP / đặt cơm. Đổi config có hiệu lực ngay không cần restart server.
- Đăng nhập chuyển hoàn toàn sang OTP qua NetChat, gỡ các view password cũ. Form thêm user: dropdown gợi ý đơn vị / phòng ban / chức vụ từ DB.
- Phân quyền: ẩn "Xuất báo cáo Excel" với người ăn (chỉ admin + nhân viên bếp).
- Tinh chỉnh CSS responsive cho mobile (4 trang chính: Dashboard / Đánh giá / Báo cáo / Profile).

---

## 📧 Liên hệ
*   **Người phát triển:** Thanghv37
*   **Đơn vị:** VT NET KV2

---
*Dự án được phát triển nhằm mục đích nâng cao chất lượng bữa ăn và tối ưu hóa quản trị nội bộ.*